# VS Code (user-specific)
.vscode/

# macOS system files
.DS_Store
**/.DS_Store

# Checkpoint system (local state snapshots)
.claude/checkpoints/

# Tests are tracked — they're template maintenance files, not project files

# Python cache
__pycache__/
**/__pycache__/
*.pyc

# Local settings
.claude/settings.local.json

# Claude workspace (temporary working documents)
.claude/support/workspace/

# Prevent nested .claude directories
.claude/.claude/