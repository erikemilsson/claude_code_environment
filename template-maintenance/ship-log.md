# Template Ship Log

Version-by-version changelog of template-maintenance ships. Extracted from root `CLAUDE.md § Active Follow-ups` on 2026-05-27 to keep the always-loaded `CLAUDE.md` under Claude Code's size budget (the changelog alone was 46k of the file's 60k chars).

Each entry records: version + date, what shipped, the FB/DEC it resolves, files touched, and SemVer rationale. Entries are roughly version-ordered — a few interleave out of strict order where ships overlapped in time (preserved as-is from the original log). **Append new ships here**, not in `CLAUDE.md`.

This file does **not** ship to projects — it's template-maintenance history. See also: `template-maintenance/feedback.md` (active FB queue), `decisions/` (template decision records), and root `CLAUDE.md § Active Follow-ups` (forward-looking deferred work).

---

- **v3.4.0 (2026-05-13)** — DEC-010 Option C: `partial_completion` envelope for usage-limit graceful resume (`decisions/decision-010-partial-completion-envelope.md`). Promoted FB-049.
- **v3.5.0 (2026-05-13)** — DEC-011 Option ABp: hybrid A+B + `.pending-markers.jsonl` transient buffer for Track 1 pipeline reliability (`decisions/decision-011-track1-pipeline-execution.md`). Promoted FB-057.
- **v3.6.0 → v3.12.0 (2026-05-15, 7 commits)** — Audit family: 6.5 of 7 stages from `template-maintenance/audit-command-family-proposal.md`. Dashboard slim (META whitelist + Recent Activity cap); friction register at `.claude/support/friction.jsonl`; commands `/audit-coherence` (6 lenses) + `/audit-ui` (7 lenses + mobile) dispatched by `/health-check` Part 8; persistent `🔍 Audit Findings` dashboard section with `[Promote to FB] / [Dismiss]`; `[Fix it]` inline-apply for `bundle-eligible` kind only per DEC-013 Option C (`decisions/decision-013-audit-fix-it-autonomy-boundary.md`).
- **v3.14.0 → v3.14.2 (2026-05-16)** — FB-003 feature-retirement workflow rule promoted; FB-062 + FB-063 cheap actions shipped; FB-060 Phases 1 + 5 (Cross-Project Capture Protocol in `.claude/rules/agents.md` + new `.claude/support/reference/extension-hooks.md`).
- **v3.15.0 (2026-05-16)** — DEC-014 Option F: `.claude/.sync-state.json` sidecar + `/health-check` Part 5 2-condition algorithm refinement (`decisions/decision-014-sync-state-and-file-ownership-categories.md`). Closes FB-059; partially closes FB-060.
- **v3.16.0 (2026-05-16)** — FB-065 + FB-066 promoted: 5th heuristic on FB-058's Pre-Pass table (decomposition-time enum-extension ripple inference) + production-consumption check in verify-agent Step T5 (catches class-export integration gaps).
- **v3.17.0 (2026-05-16)** — FB-064 promoted: Test-Harness Awareness section added to decomposition.md + SKILL.md (mirror). Detects runtime-shaped tasks (`interaction_hint: cli_direct`, runtime-surface globs, runtime/UI `test_protocol`); scans for conventional harness directories with root `./CLAUDE.md` opt-in for custom paths; proposes scenario-authoring `_h` subtask when harness exists. Bridged from echothread `FB-009`.
- **v3.17.1 (2026-05-16)** — FB-006 PATCH iteration 1: dropped per-finding `[Promote to FB] / [Dismiss]` inline text from dashboard audit-findings render (closes FB-006 sub-issue 3 "dead UI"). `[Fix it]` retained for bundle-eligible (no keyword alternative); non-bundle-eligible kinds show italicized kind annotation only. `dashboard-regeneration.md` § "Audit Findings sub-section" + `dashboard-style/SKILL.md` mirror + `audit-fix-workflow.md` example updated; new "How to act on findings" sub-section documents promote (tick + bulk CLI) and dismiss (natural-language) invocation patterns. Plain-English description (sub-issue 4) + `/audit-coherence triage` (sub-issues 1+2) + dual-checkbox column deferred to future MINOR iterations.
- **v3.18.0 (2026-05-16)** — FB-006 MINOR iteration 2: added `description` field to `digest.json items[]` (Component 2 schema) for plain-English at-a-glance dashboard triage (closes FB-006 sub-issue 4 "opacity at decision moment"). Synthesizer prompts in `commands/audit-coherence.md` + `commands/audit-ui.md` add Algorithm step 4 instruction: complete sentence, period-terminated, ~80-140 chars, names the affected artifact, distinct from `title` (which stays as terse cluster identifier for findings.md headers + FB titles on promote). Render rules in `dashboard-regeneration.md` + `dashboard-style/SKILL.md` mirror use `{description ?? title}` for backward-compat with pre-v3.18.0 digest.json files. `audit-command-family-proposal.md` § Component 2 schema example shows new field; `audit-fix-workflow.md` example block updated with plain-English descriptions.
- **v3.19.0 (2026-05-16)** — FB-006 MINOR iteration 3: `/audit-coherence triage [audit-ts]` + `/audit-ui triage [audit-ts]` interactive walkers shipped (closes FB-006 sub-issues 1 "courier pattern" + 2 "audit-name memory burden"; all four FB-006 sub-issues now closed). `audit-ts` defaults to `latest` (newest `coherence-*`/`ui-*` audit dir by `ran_at`) — user never types the audit name. Walker iterates pending non-dismissed findings (`status: pending` AND id NOT in sidecar `dismissed_ids[]`), renders each as `[i/N] {C-ID} ({kind})` + `description ?? title` (pre-v3.18.0 fallback) + `Files to touch` + `Source anchors` + kind-conditional `Actions:` prompt, reads user response (single-letter `F/P/D/S/Q` or natural-language verbs with reason inline), dispatches to canonical per-action mechanics in `audit-fix-workflow.md` (no divergence). Auto-advance between findings; `[Q]uit` available at every prompt; `Ctrl+C` mid-walk is safe (completed actions stay committed, remainder stays pending). Cross-refs: `commands/audit-coherence.md § "Triage mode"` (H2, full algorithm) + `commands/audit-ui.md § "Triage mode"` (H3 under Workflow, parallel-by-substitution with C→F prefix); `support/reference/audit-fix-workflow.md` "Preferred entry point for N pending findings" section at top of Action protocol; `support/reference/dashboard-regeneration.md` + `skills/dashboard-style/SKILL.md` mirror "How to act on findings" sub-section adds "Preferred for multiple findings" bullet. Dual-checkbox promote-vs-dismiss column closed as obsoleted by triage walker.
- **v4.0.0 (2026-05-16)** — **MAJOR**: DEC-016 Option D promoted — Claude direct-edit guardrail on spec/decision/vision files via composite rule + permission-layer enforcement (closes FB-007). **Inflection point**: first template-wide `permissions.ask` shipping in template-owned `.claude/settings.json` — the convention now is "base `permissions.allow` AND base `permissions.ask`" for the file. Six `permissions.ask` globs added (Edit + Write × `.claude/spec_v*.md`, `.claude/support/decisions/decision-*.md`, `.claude/vision/**/*.md`) — symmetric with DEC-013's audit-system protection. New `## Direct edits to spec, decision, and vision files (DEC-016)` section in `rules/spec-workflow.md` replaces the "direct edits always safe" line with explicit routing rule (substantive text edits MUST route through `/iterate` or analogous per category; infrastructure operations like archiving/version transitions/frontmatter remain autonomous). `.claude/CLAUDE.md` Critical Invariants updated (two invariants: settings layering now mentions both base sets; new invariant points to the spec-workflow section). Cross-reference updates: `sync-manifest.json` notes, `.claude/README.md` settings description, `support/reference/extension-hooks.md` settings-layering reference, `commands/health-check.md` Part 5c (allows `permissions.ask` as template-owned key; drift comparison expanded to both arrays; rationale prose updated). Decision record at `decisions/decision-016-spec-file-edit-guardrail.md` (status: implemented; 7 implementation_anchors). Research archive at `decisions/.archive/decision-016-research-2026-05-16.md`. Per-amendment user cost: one click per `/iterate apply` (or `/research`) session at the first Edit; platform-native "Yes don't ask again" persists for session.
- **v4.1.0 (2026-05-20)** — FB-071 promoted: `disable-model-invocation: true` frontmatter applied to all 5 strong-candidate commands (`/breakdown`, `/research`, `/iterate`, `/work`, `/feedback`). Blocks Claude's autonomous invocation via the `Skill` tool while preserving user-typed slash invocation. **Live skills-list verification immediate** (during the ship session): the model-invocable template-commands set shrank from 10 → 5 — the 5 strong candidates were removed, while the 5 weak candidates (`/status`, `/health-check`, `/review`, `/audit-coherence`, `/audit-ui`) remained ambient-invokable as designed. New `## Command Invocation Gates` section in `.claude/rules/agents.md` (between `## Behavioral Rules` and `## Cross-Project Capture Protocol`) documents convention + selection criteria + sub-mode coupling trade-off (multi-mode files gate as a whole — read-only sub-modes like `/work` no-args also lose ambient invocation; user-typed invocation still works; future option to split files if friction surfaces) + defense-in-depth with DEC-005 (auto-mode permission layer) and DEC-016 (spec/decision/vision Edit/Write ask). Medium candidates (`/iterate distill`, `/iterate propose`, `/audit-coherence triage`, `/audit-ui triage`) deferred per FB-071 design — re-evaluate after a few weeks of strong-candidate gating in practice. No new files; no sync-manifest change. Single commit; no DEC.
- **v4.2.0 (2026-05-20)** — FB-068 promoted: new `/grill` command (interview-style interrogation, branch-by-branch decision-tree walking, one question at a time, recommend an answer each) + `./CONTEXT.md` slot (project-owned, lazy-created domain glossary at project root). Adapted from `mattpocock/skills/engineering/grill-with-docs` — preserves the with-docs flow (challenge fuzzy language, sharpen terms, cross-reference code, update CONTEXT.md inline, offer ADRs sparingly with 3 criteria) and integrates with CCE's `/iterate` + `/research` routing + DEC-016 guardrail (no direct writes to spec/decision/vision). Integration points: `./CONTEXT.md` row added to `.claude/CLAUDE.md` Navigation table; `/grill` in Environment Commands; new `## Domain Glossary Awareness` section in `rules/agents.md` (placed after `## Root Cause Over Symptom`, before `## Behavioral Rules`) — codifies implement-agent / verify-agent / maintenance / layer-distinction rules; step 3 added to `rules/spec-workflow.md § Vision Documents` as optional pre-distill enrichment; `audit-coherence.md § "Lens 2 — vocab-drift"` extended to consume CONTEXT.md as canonical naming reference when present. Live skills-list verification: `grill: Grill Command` appeared immediately. All FB-068 explicit out-of-scope items honored: no starter CONTEXT.md shipped (per Pocock's deprecation lesson — placeholders don't get filled); no batch-extract from spec/code; no `CONTEXT-MAP.md` for monorepos; no `/grill` direct writes to `decisions/` (routes to `/research` instead); no glossary fingerprinting; no co-equal source-of-truth machinery. Ship-time deviation from capture: original plan said "wire as `/iterate grill` sub-mode"; shipped as standalone command referenced from spec-workflow.md to avoid bloating `iterate.md` (already gated as a whole under FB-071). `/grill` does NOT carry `disable-model-invocation: true` — the interactive one-question-at-a-time pattern is the safeguard; Pocock's `grill-me` doesn't gate either.
- **v4.3.0 (2026-05-20)** — FB-070 promoted: new `/zoom-out` micro-command (~50 lines, single file). Adapted from `mattpocock/skills/engineering/zoom-out` (Pocock's 7-line skill); domain-genericized for CCE (software, research, procurement, renovation — any spec-driven project). Consumes `./CONTEXT.md` vocabulary when present (FB-068 integration); degrades gracefully when absent. Carries `disable-model-invocation: true` frontmatter on day one per FB-071's convention — `/zoom-out` is a user-asks-for-help signal where autonomous fire would be circular (the model would be invoking it for its own benefit, which doesn't match the help-the-user trigger). Pocock's `/zoom-out` carries the same frontmatter for the same reason. No sync-manifest change (`.claude/commands/*.md` glob auto-covers); no DEC needed. Also: **FB-072 captured** in `template-maintenance/feedback.md` — research-first investigation of command routing as a UX pattern (interpretive vs explicit-arg dispatch + boundary survey across `/iterate` / `/work` / `/research` candidate umbrellas). Trial-gated DEC candidate. Surfaced during the FB-068 ship review: user observed that `/grill` as a standalone command adds UX surface, and proposed `/iterate` become an interpretive router that dispatches sub-purposes from natural language. FB-072 documents the pattern, the survey deliverable, the research questions, and the trial-gate criteria; no implementation until survey + prototype + effectiveness data accumulate.
- **v4.4.0 (2026-05-20)** — FB-069 promoted: new `/diagnose` command (6-phase debugging methodology: feedback loop → reproduce → falsifiable hypotheses → instrument → fix + regression test → cleanup + post-mortem; ~170 lines). Adapted from `mattpocock/skills/engineering/diagnose`. Domain-genericized for CCE — the methodology applies to any "something is wrong, I don't know why" task, not only software bugs. Phase-1 feedback-loop discipline + Phase-3 falsifiable-hypothesis discipline are the structural enforcement mechanisms for `agents.md § "Root Cause Over Symptom"` on multi-turn debugging (the Phase-3 discipline is specifically what prevents the "swap-and-see" pattern that produces symptom-only fixes). Phase-6 architectural-friction handoff routes through CCE's friction register (`design_contradiction` kind) or `/research` for substantial architectural decisions — no `/improve-codebase-architecture` exists yet (FB-067 Wave 2). Cross-references shipped: (a) `agents.md § "Root Cause Over Symptom"` extended with a paragraph routing hard-bug cases through `/diagnose` (mutual reference); (b) `spec-workflow.md § "Workflow Cycle"` extended with a "Bug tasks" paragraph naming `/diagnose` as the preferred route. **Leaves `disable-model-invocation: true` OFF** per the FB-071 selection criteria (three options discussed: gate now / leave open / add to medium candidates; chose leave-open because autonomous-fire-when-stuck is exactly the value — when implement-agent hits a hard bug mid-`/work`, sweeping into structured methodology is the feature, not the foot-gun; can flip to gated later if observed misfires accumulate). No sync-manifest change (`.claude/commands/*.md` glob auto-covers); no DEC needed. Also: **FB-072 candidate list extended** with "Help-me-think family" umbrella (`/zoom-out`, `/grill`, `/diagnose`) — three help-me-think commands now exist; worth surveying whether a single `/help` or `/think` entry point + interpretive dispatch produces better UX than three discrete commands. **Wave 1 complete** — FB-068 + FB-069 + FB-070 + FB-071 all shipped 2026-05-20 across v4.1.0 → v4.4.0.
- **v4.5.0 (2026-05-20)** — FB-073 promoted (Wave 2 start): new `## Test-Protocol Runtime Constraints` section in `.claude/skills/decomposition-heuristics/SKILL.md` (sibling to existing Test-Harness Awareness section). Covers detection patterns for test_protocol steps that exercise runtime behavior not testable under the project's primary phone runtime (Expo Go's offline cold-launch limitation is the canonical example — Expo Go fetches its JS bundle from Metro on every cold-launch, so airplane-mode cold-launch freezes Expo Go itself regardless of in-app cache correctness). Three substitution patterns: background mode (lock → unlock, JS stays in memory), server-only kill (dev server stopped + WiFi on, app force-quit + relaunch — tests fetch-failure → cache-render branch), defer-to-dev-client (post-EAS). Annotation pattern via `constraint` informational field on test_protocol steps; documented as informational only (formal schema field deferred until convention proves itself). Optional project-side `**Primary phone runtime:**` declaration in root `./CLAUDE.md`. Cross-reference added to `.claude/support/reference/task-schema.md § Test Protocol Field` (new `### Runtime Constraints` sub-section pointing at the skill). Source: styler FB-174 bridge (iPhone attestation walkthrough hit the limitation). No DEC; no sync-manifest change.
- **v4.6.0 (2026-05-20)** — FB-074 promoted (bundled three sub-issues, FB-006 precedent): (1) **Canonical decision categories extended** with `ux`, `design`, `ui-ia`, `ui-content` in `.claude/support/reference/decisions.md` (template enum line 22 + Categories table with one-sentence definitions) AND `.claude/commands/health-check.md` Part 3 check 1 (validation enum). Addresses 22/100 styler decisions whose categories fell outside the previous 6-value set; `data` category deferred per FB-074 body's "lower priority" annotation. (2) **Parent-task aggregate-subtask verification exception** added to `.claude/commands/health-check.md` Part 1 check 7 alongside the existing `owner: human` self-attested exception. Pattern: parents with status `"Broken Down"` (non-empty `subtasks`) OR `"Finished"` (non-empty `subtasks` where every subtask is itself `"Finished"` with passing per-task verification) that have `checks.aggregate_subtask_verification: "pass"` are exempt from the 7-key requirement. Pattern-mirror of existing exception; no schema change. (3) **Rules-file soft cap raised 200 → 220** in `.claude/commands/health-check.md` Part 2c. Closes the standing warning on `.claude/rules/feature-retirement.md` (203 lines, genuinely rich procedure — procedure + 5 edge cases + restore path + worked examples). Source: styler FB-192 bridge. No DEC; no sync-manifest change. Also: **FB-077 captured** in `template-maintenance/feedback.md` — auto-mode classifier false-positives surfaced in-session: (a) classifier blocked Edit to `.claude/support/reference/decisions.md` citing DEC-016, but DEC-016 only covers spec/decision-record/vision files — not the reference doc *about* decision format; (b) AskUserQuestion responses don't count as "visible user response authorizing the retry" — classifier requires typed-text. Resolved with explicit typed-text user authorization. FB-077 captured for upstream-Anthropic / DEC-005 follow-up.
- **v4.6.1 (2026-05-20)** — FB-075 cheap action shipped: "Harness reminders about built-in task tools" paragraph added to `.claude/README.md § Known Constraints` documenting that the TaskCreate / TaskUpdate harness-emitted reminder is unavoidable and benign in projects using `.claude/tasks/*.json` (the reminder is harness-emitted, not template-emitted; agents ignore it automatically; noise is visible but benign). Empirically observed in this very session 2026-05-20 during inbox triage — the harness reminder fired multiple times including immediately after the AskUserQuestion confirming FB-075's capture (noted verbatim in FB-075's Source line). Structural fix (per-project opt-out) requires Anthropic-side mechanism and remains upstream-gated; FB-075 stays in `template-maintenance/feedback.md` as `cheap-action-shipped + deferred`. Also: **FB-076 deferred** (formalized) — verify-agent runtime_validation gaps (bundle-boundary breaks + catalog-state-dependent precondition gaps from styler T667). Mitigations (`npm run build` invocation, ESLint client-import rule, live-data cross-reference) require design work less mechanical than FB-066's regex+grep; single-project signal doesn't justify it yet. Re-assess on 2nd project signal or FB-066 downstream telemetry; if escalated, route through `/research`. No DEC; no sync-manifest change.
- **v4.6.3 (2026-05-20)** — FB-077 cheap action shipped: "Auto-mode classifier false-positives" paragraph added to `.claude/README.md § Known Constraints` (sibling to FB-075's TaskCreate harness reminder paragraph). Documents two observed runtime-classifier behaviors that can block legitimate work: (a) **DEC-016 scope over-broad** — edits to `.claude/support/reference/decisions.md` may be blocked citing DEC-016 even though DEC-016 only covers spec/decision-records/vision files; workaround: lead with explicit context-clarifying language ("this is the reference doc, not a decision record"), or user provides typed-text authorization; (b) **AskUserQuestion responses don't count as classifier-bypass authorization** — the classifier requires typed-text "visible user response"; workaround: agents prefer free-text prompts (not AskUserQuestion) when classifier-bypass authorization is the goal. `AskUserQuestion` remains appropriate for collecting preferences and structured decisions. Both behaviors are upstream-Anthropic (classifier lives in the runtime, not the template); structural fixes remain upstream-gated. FB-077 stays in `template-maintenance/feedback.md` as `cheap-action-shipped + deferred`. No DEC; no sync-manifest change.

- **v4.6.2 (2026-05-20)** — FB-011 Family E permanently dropped. 30-day trial window (2026-04-17 → 2026-05-17) closed with no FB-017 regression observed in this template repo or across three downstream projects (echothread, styler, SIREN). FB-017's inlining fix at `commands/work.md` Step 2b + `commands/iterate.md` is sufficient; decision auto-finalization does not need script extraction. `template-maintenance/scripts-candidates.md` updated to reflect Family E closure across four locations (section header + Recommendation + Trial window + Tier 3 § Recommended extraction order + Next action). Root CLAUDE.md FB-011 entry updated — Family E removed from the active re-assess list; C/D remain deferred per observed-need gates. Also: **FB-078 captured** in `template-maintenance/feedback.md` — surfaced 2026-05-20 during the Family E re-assessment as a side-discovery in styler's 2026-05-16 session export `workflow_friction_notes`. `/work` Step 2b post-decision check fires `/iterate` suggestion based on the decision-level `inflection_point` flag alone, ignoring whether the chosen option has spec impact. Two mitigation candidates documented (heuristic vs schema flag). Single-project signal; not promoted yet. Distinct from FB-017's decision-auto-finalization sub-feature — same Step 2b function, different sub-paths. No DEC; no sync-manifest change.

- **v4.6.4 (2026-05-20)** — FB-079 promoted: `/work pause` session-export filename collision fixed. Minute-granularity timestamp (`YYYY-MM-DD-HHMM`) applied to both write sites: (a) `.claude/commands/work.md` line 1057 (orchestrator pause procedure step 5 — instruction text updated to the new pattern with inline FB-079 reference), and (b) `.claude/hooks/pre-compact-handoff.sh` lines 230 + 237 (PreCompact-hook fallback path — new `timestamp = datetime.now(timezone.utc).strftime("%Y-%m-%d-%H%M")` variable; both the workspace export path AND the template-inbox copy use `timestamp` rather than `today`). The existing `today` variable continues to drive task-filtering by `completion_date` AND the `session_date` payload field (which stays YYYY-MM-DD per export schema, separate concern from filename). Direct mirror of SIREN's own build-bundles.py fix pattern (per FB-079 capture). Same-day pauses now produce distinct filenames rather than silently overwriting earlier exports. The `.session-export-*.json` glob already matches the new filename shape; no reader-side change needed (verified — no other write or glob-based read sites in the template). Captured + promoted same session (`/feedback`-style scan-then-ship loop): FB-079 captured 2026-05-20 morning during cross-project signal scan from `interaction-logs/processed/SIREN-task-7.5-session-export-2026-05-18-T1530.json`; shipped 2026-05-20 afternoon in v4.6.4. No DEC; no sync-manifest change (the two affected files are already in `sync` category; diff is content-only). Also: **FB-080 + FB-081 captured** (from same scan) — Dashboard full-regen too heavy for incremental updates (multi-session signal: styler 2026-05-17 + echothread 2026-05-17; two routes: partial-regen mode vs formalize defer-then-regen pattern), and long autonomous batches (3+ implement+verify cycles) lack heartbeat or user-check-in default (single-session signal but tied to concrete observable friction event; two patterns documented: heartbeat between dispatches + ping-mid-batch behavior rule). Both remain `new` in `template-maintenance/feedback.md`; signal-queue note in same file captures six additional weaker signals for next-session triage. Also: **`.claude/support/workspace/router-survey.md`** drafted as FB-072 deliverable 1 — boundary survey of five candidate router umbrellas (`/iterate`, `/work`, `/research`-vs-`/iterate` overlap, `/audit-*`, help-me-think family); net recommendation: prototype interpretive routing on `/iterate` only; defer others. Survey is a workspace draft, not a template ship — FB-072 itself stays `ready` pending user response to the three decision points at the bottom of the survey.

- **v4.7.2 (2026-05-23)** — Dashboard format-example refresh (PATCH, no FB — surfaced via `/health-check` Part 1 format-staleness check). The shipped placeholder dashboard at `.claude/dashboard.md` was last regenerated under template v3.0.0 and had drifted from current `skills/dashboard-style/SKILL.md` rules in two places: (a) META block missing 6 of the 13 whitelisted fields (`spec_version`, `spec_status`, `decision_count`, `decisions_approved`, `decisions_superseded`, `decisions_partially_superseded`) plus `template_version` stale at 3.0.0; (b) Decisions table Selected column not following the "always link, Decided shows option-name as link text, Pending shows 'Pending' as link text" rule from `dashboard-style/SKILL.md § "Section Display Rules"` — DEC-001's selected option was plain text and DEC-002 used "Review options" as link text instead of "Pending". Both fixed. META field order now matches the SKILL.md Step 4 canonical order (task_count before task_hash; spec_version + spec_status after task_hash; decision counts at the end). No body content changes — the renovation narrative dates (Jan 2026) stayed intact since they form a coherent fictional mid-execution snapshot. Audit Findings sub-section + Recent Activity sub-section deliberately not added — both auto-render only when data exists and the renovation example has neither (auditing the rules would have meant inventing fake findings, which teaches the format but pollutes the example narrative). No sync-manifest change (`.claude/dashboard.md` is in `ignore` category — doesn't sync to downstream projects post-clone, but does ship on initial clone, so the refresh propagates to all new projects from this point). Single commit; no DEC. Also: **FB-083 captured** in `template-maintenance/feedback.md` — capability grounding for spec/agent/model/skill design + freshness mechanism for `.claude/support/reference/claude-code-capabilities.md` (or merged-doc alternative). Research-gated; paired with FB-082 (the two together suggest a broader "Claude Code authoring hazards" template surface). Source: flirty-gym 2026-05-22 session export (now in `interaction-logs/processed/`). Likely DEC candidate after research — the freshness mechanism (WebFetch sync vs cadence lens vs version-pinned + manual vs footer) is the substantive decision; the capability reference content is more mechanical once the mechanism is chosen.

- **v4.7.0 (2026-05-20)** — FB-080 promoted: dashboard regen cost vs deferral pattern resolved via Route C1 (hybrid: targeted-edit pattern + sidecar sentinel). Selected over Route A (section-fingerprints in META — substantial, grows the "strict 13" whitelist, risk of dirty-tracking bugs) and Route B (defer-everything-to-session-boundary — cheap but throws away mid-session value Erik is already getting from informal targeted edits). Research analysis in `.claude/support/workspace/fb-080-research.md`. **What shipped (six files):** (a) `.claude/skills/dashboard-style/SKILL.md` — new `pending_full_regen: String | null` field added to sidecar schema (JSON example + field-definitions table) + new top-level section "Targeted Edits (mid-session lite path)" inserted between "When to Regenerate" and "Regeneration Steps" with 10-pattern decision table (4 targeted-eligible: single phase decision, single task→Action Required, META timestamp refresh after status flip, format-staleness fix touching only META; 6 require-full-regen: decomposition complete, parallel batch end, `/work complete`, session boundaries, multi-task changes ≥3, spec version transitions), 4-step procedure (`Edit` affected section → recompute `task_hash` + refresh `generated` timestamp in META → set sentinel in sidecar → skip 8-step regen procedure), cycle-and-clearing rules, rationale paragraph; brief pointer added after Tier 2 parallel-mode note. (b) `.claude/support/reference/dashboard-regeneration.md` — parallel mirror of all SKILL.md changes (same insertion points, identical content). (c) `.claude/commands/work.md § "Step 1a: Dashboard Freshness Check"` — new paragraph instructing orchestrator to read `pending_full_regen` field and trigger full regen if non-null. (d) `.claude/support/reference/drift-reconciliation.md § "Dashboard Freshness Check"` — added check 5 (sidecar sentinel) to the freshness-check procedure; "Why this matters" paragraph extended to mention three staleness modes (content / format / drift). (e) `.claude/rules/dashboard.md § "Regeneration Strategy"` — one-paragraph pointer to the new SKILL.md section. (f) `.claude/version.json` — 4.6.4 → 4.7.0. **Behavior change:** before, Tier 1 triggers fired full regen and orchestrators informally deferred + did targeted edits anyway (observed twice-repeated in styler + echothread session exports); after, sanctioned targeted-edit path is documented with explicit decision table + sentinel-backed guarantee that Step 1a will catch any accumulated drift. **Compatibility:** missing `pending_full_regen` field treated as `null` (back-compat for sidecars created before this convention). No DEC (route selection bundled into ship per research-light deliverable). No sync-manifest change (all six files already in `sync` category). MINOR per SemVer policy (new sanctioned behavior surface; no breaking change). Route A remains available as future iteration if Route C1 produces residual friction.

- **v4.10.0 (2026-05-24)** — `/grill` no-args triage flow added (single-session ship via in-conversation grill of the design itself; design walked end-to-end with explicit recommendations + counter-evidence at each branch). `/grill` is now two-mode: explicit-topic flow (`/grill {topic}` or `/grill {vision-file}`) drills directly per existing Process content; no-args flow (`/grill`) scans project state (default scope: spec + decisions + recent FB items + `./CONTEXT.md` when present; widens to code if those don't surface enough), surfaces 2-4 candidate areas via AskUserQuestion with "(Recommended)" appended to the suggested starter (first in list per the tool's convention), then drops into the explicit-topic flow at the picked area. Edges documented: zero good candidates → suggest explicit-topic invocation; multiple in sequence → model references the original triage list if user wants to drill another. **Files touched (5):** `.claude/commands/grill.md` (lede + Usage no-args line + 5th When-to-Use item + Process restructured into two sub-flows), `.claude/CLAUDE.md` (Environment Commands `/grill` row), `.claude/version.json` (4.9.0 → 4.10.0), `.claude/dashboard.md` (META bump), root `CLAUDE.md` (this entry). MINOR per SemVer (new behavior surface for the no-args case; no breaking change to explicit-topic invocation). No DEC (design decisions made in-session via `/grill`'s own discipline: γ two modes, Y format-only spec, P AskUserQuestion structured pick, II default scope listed with widen-to-code escape hatch). No sync-manifest change (`.claude/commands/*.md` glob auto-covers). Ship-process note: 3-commit per-ship surgery (v4.8.0 + v4.9.0 + v4.10.0) since prior session ships were uncommitted in working tree; backup+edit+add+restore mechanism used in lieu of `git add --patch` (interactive, unsupported in headless conversations).

- **v4.10.1 (2026-05-24)** — FB-090 cheap action shipped (same-session capture → promotion → ship during a `/feedback review` walk-through; FB-084 / FB-087 precedent). Made the dashboard Recent Activity 7-entry cap **enforcement non-discretionary** and added cap-trim as a sanctioned targeted-edit pattern, now that FB-080 (v4.7.0) made single-section trims cheap. **Files touched:** two edits each to `.claude/skills/dashboard-style/SKILL.md` + its mirror `.claude/support/reference/dashboard-regeneration.md` — (1) new "Recent Activity cap-trim (drop entries above the 7-entry cap)" row in the "Targeted Edits (mid-session lite path)" decision table; (2) new "Cap enforcement is non-discretionary (FB-090)" sub-bullet on the Recent Activity Section Display Rule, tying cap-trim to any edit that touches the section (full regen OR targeted edit) and removing the regen-cost excuse. Plus `.claude/version.json` (4.10.0 → 4.10.1) + `.claude/dashboard.md` META bump. PATCH per SemVer (tightens an existing cap + the existing FB-080 targeted-edit table; no breaking change — consistent with v4.7.3's cheap-action rule additions). No DEC. No sync-manifest change (both content files already in `sync`). FB-090 was promoted from the 2026-05-20 signal queue once its blocker (FB-080) had shipped; full entry archived in `template-maintenance/feedback-archive.md`, active stub left in `feedback.md`. Same `/feedback review` pass also **kept** the "math-check before commit-to-pixels" signal-queue item queued (single-project + niche; held for a 2nd-project signal).

- **v4.10.2 (2026-05-25)** — `/diagnose` Phase 3 sharpen: **assert the outcome, not the mechanism**. New paragraph in `.claude/commands/diagnose.md § "Phase 3 — Hypothesise"` (between the falsifiable-prediction format and the "show the ranked list" checkpoint) requiring each hypothesis's prediction to be phrased as an observable end-state ("after X, measured value Y within tolerance T"), never as a mechanism claim ("because Z re-evaluates mid-operation"). Rationale: a mechanism-shaped prediction can be rubber-stamped by a reviewer who shares the same wrong model — writer + reviewer agree and both are wrong (the FB-085 writer/reviewer shared-premise blindspot); an outcome-shaped prediction stays falsifiable regardless of mental model and fails on its own when the mechanism belief was wrong (would have caught styler's T697 anchor-scroll case regardless of domain). **General-merit** — applies to all bug classes, not just UI. **Provenance:** outcome of the `/visual-verify` grill (2026-05-24, `template-maintenance/visual-verify-vision.md`), which concluded the proposed command should **fold into `/diagnose`** rather than add new surface (FB-072/DEC-018 command-surface discipline). This Phase 3 sharpen is the one general-merit piece extracted now; the UI-specific browser-loop recipe stays captured in FB-085 (deferred, signal-gated), ready to ship on a 2nd-project signal. **Files touched:** `.claude/commands/diagnose.md` (Phase 3 paragraph) + `.claude/version.json` (4.10.1 → 4.10.2) + `.claude/dashboard.md` (META `template_version` bump); plus capture-only edits to `template-maintenance/feedback.md` (FB-085 § "Resolved design") + `template-maintenance/visual-verify-vision.md` (resolution note). PATCH per SemVer (wording/discipline sharpen to an existing command; no new surface, no breaking change — consistent with v4.10.1 / v4.7.3 cheap-action precedent). No DEC (the grill confirmed the fold fails the hard-to-reverse criterion). No sync-manifest change (`.claude/commands/*.md` glob auto-covers).

- **v4.11.0 (2026-05-27)** — **DEC-019: new `/shakedown` command** — acceptance-by-example capability-boundary mapping. Surfaced from two styler transcripts (FB-093), grilled as a design (`template-maintenance/shakedown-workflow-vision.md § Resolution`), formalized via `/research` into `decisions/decision-019-shakedown-command.md` (Option A: **standalone**, over fold-into-`/grill` / pipeline / pattern-doc). **What it is:** the mirror of `/grill` — grill drills *down* to sharpen what you *mean*; `/shakedown` sweeps *across* to test the built/envisioned system against real-use examples (typical + edge), grounding each against the actual system and verdicting (✓/⚠/✗/❓) into a dated, snapshot-anchored corpus that is triple-duty (gap analysis + acceptance probes + forward-direction map). **Phase 0 is a required directed grill** (cross-refs `grill.md § Process`) that calibrates the lens — what the session tests for + the relevant parts of the system + a non-thin lens — before any example; that *requirement* is what ruled out the lightweight-sub-mode-of-grill framing (can't fold X into Y when X requires Y as a precondition). Then the probe loop: restate → break down → **ground against the actual system with a precise *why*** → verdict → write immediately; maintain the model between examples; proactively steer; stop at saturation; route ⚠ → `/iterate` or `/research` (proposes, never writes spec — DEC-016). Domain-agnostic (lens derived per-project at Phase 0; styler's mechanism/bite/direction/when/unless is illustration, not baked in). Ungated (like `/grill`/`/diagnose` — interactivity is the safeguard). **Files (8):** new `.claude/commands/shakedown.md`; new `.claude/support/shakedowns/README.md` (artifact-home convention — dir + README ship, contents project-generated, **no template `.gitignore` entry, mirroring `support/audits/`**); `.claude/sync-manifest.json` (README registered; `commands/*.md` glob auto-covers the command); `.claude/CLAUDE.md` (Navigation + Environment Commands rows); `.claude/rules/spec-workflow.md § "Workflow Cycle"` (working-backward pointer); `.claude/commands/grill.md § "Where it fits"` (inverse-sibling cross-ref); `.claude/version.json` (4.10.2 → 4.11.0); `.claude/dashboard.md` (META bump). MINOR per SemVer (new command surface; no breaking change). **DEC-019** (status `implemented`) carries the `/grill`-vs-`/shakedown` boundary + n=1-evidence honesty (built now because net-new + ungated + reversible; Re-open/down-grade condition = fold into `/grill` à la `/visual-verify` if it underperforms). **Feasibility check** (against `claude-code-authoring.md`, DEC-017): buildable as a main-conversation command (so it writes its own doc + runs the loop outside the subagent `.claude/`-write sandbox); body written as standing patterns, compaction-safe. FB-093 → PROMOTED. **Process note:** the full chain ran in one session — FB capture → `/grill` the design → `/research` → DEC-019 → build+ship — the first end-to-end *grill-the-design → research → ship-a-new-command* run (vs `/visual-verify`'s grill-concludes-fold).

- **v4.9.0 (2026-05-24)** — DEC-017 Option B shipped: capability authoring doc + `/health-check` freshness lens. Decision record at `decisions/decision-017-capability-authoring-doc-freshness.md`; research at `.claude/support/workspace/fb-082-083-research.md`. **What shipped (10 files):** (a) new `.claude/support/reference/claude-code-authoring.md` — 5 sections (YAML Frontmatter Hazards, Skill Frontmatter Scope, Subagent Boundaries, Tool & Dispatch Surface, MCP Constraints) + footer with `Last verified against Claude Code docs @ YYYY-MM-DD; against template_version: X.Y.Z`. Consolidates facts not surfaced anywhere (turn-scoped `model:`/`effort:` frontmatter, YAML colon-space hazard, Agent tool model granularity, skill content lifecycle, auto-compaction 25K re-attachment budget, skill listing 1,536-char cap, forked-skill context inheritance) and cross-links scattered facts (subagent isolation, MCP constraints, dispatch convention) without consolidating. (b) `.claude/sync-manifest.json` — registered in `sync` array. (c) `.claude/CLAUDE.md § Navigation` — new row pointing to authoring doc. (d) `.claude/agents/implement-agent.md § "Editing strategy"` — YAML colon-space bullet (em-dash convention) with cross-ref. (e) `.claude/rules/agents.md` — capability-grounding cross-refs in § State Ownership + § Command Invocation Gates. (f) `.claude/commands/iterate.md § "Step 4: Propose Changes"` — capability-claim cross-check before proposing spec text. (g) `.claude/commands/work.md § "If Decomposing"` — capability-claim cross-check during decomposition. (h) NEW `.claude/commands/health-check.md § "Part 2d: Capability Doc Freshness"` — reads footer; 90-day warning threshold + 180-day strong warning; `[V] Verify` user-adjudicated WebFetch action diffs against current doc body + user accepts/rejects per section; `[D] Defer` writes sentinel to `dashboard-state.json` for 30 days. (i) `.claude/version.json` — 4.8.0 → **4.9.0** (MINOR). (j) `.claude/dashboard.md` META — `template_version` bump. **Why Option B over alternatives:** Option A (footer-only) ships visible accountability without active mechanism — worst-of-both. Option C (WebFetch auto-sync) carries silent-contradiction risk between synced doc and current spec — auto-sync's immediacy value is also its failure mode. Option D (version-pinning) decouples doc cadence from Claude Code's release cadence — high maintainer burden. Option B preserves user adjudication authority while delivering active drift signal at the existing `/health-check` cadence. **Deferred:** `/audit-coherence` capability-claim lens (retroactive sweep over spec text) — Wave 2 if signal accumulates. Active feedback.md FB-082 + FB-083 replaced with PROMOTED stubs pointing to archive entries.

- **v4.8.0 (2026-05-24)** — 5-FB cheap-action bundle from `/feedback review` manual walk-through on `template-maintenance/feedback.md` (FB-006 / FB-074 / v4.7.3 multi-cheap-action-ship precedent). Per-research dispositions in `.claude/support/workspace/fb-{078,081,086,088,089}-research.md`. **FB-078** — `phase-decision-gates.md § "Post-Decision Check"` extended with Step 2a.1 chosen-option no-op scan (4 canonical markers + contradicting-phrase guard). Catches the false-positive case where `inflection_point: true` decisions land on close/defer/no-op options. **FB-081** — heartbeat (`commands/work.md § Step 3 "Autonomous batch heartbeat"`) + ping-mid-batch behavioral rule (`rules/agents.md § "Behavioral Rules" — "Acknowledge mid-batch user messages"`) shipped together, sharing `autonomous_batch_position >= 3` threshold. Heartbeat is Tier 2 inline only (NOT dashboard write — violates Recent Activity 7-entry cap). **FB-086** — verify-agent.md Step T2b extended with step 4b distinguishing drift (same-directory undeclared + `[Multi-file]` flag) from violation; emits `verification_gap` friction marker; `commands/work.md § "After verify-agent returns" step 8` auto-updates task JSON's `files_affected` to match actual; `friction-register.md` extended `verification_gap` row with sub-use note. Pre-pass parallel surface (FB-058 6th heuristic) deferred — verify-time check has ground truth. **FB-088** — new `commands/work.md § "Step 0e: Uncommitted-Work Check"` between Step 0d and Step 1; always runs but surfaces only when N≥3 finished tasks since last commit AND non-zero modified/untracked source files; heuristic-only `.claude/` exclusion filter; dashboard sentinel deferred. **FB-089** — new `commands/work.md § "Step 0f: Track 2 Stale-File Recovery"` after Step 0e; Option 1.5 (recover-by-compile-then-cleanup) shipped; new `export_quality: "recovered"` enum value; orphaned `.interaction-assessment.json` + `.session-log.jsonl` compiled into `.session-export-{timestamp}-recovered.json` then deleted; no PreCompact hook coordination needed (disjoint Track 2 territory). **Files touched (7):** `.claude/support/reference/phase-decision-gates.md`, `.claude/commands/work.md` (4 sections), `.claude/rules/agents.md`, `.claude/agents/verify-agent.md`, `.claude/support/reference/friction-register.md`, `.claude/version.json`, `.claude/dashboard.md` (META `template_version` bumped 4.7.3 → 4.8.0). MINOR per SemVer (new behavior surfaces — heartbeat, ping-rule, Step 0e, Step 0f, T2b drift detection; no breaking change). No DEC (per-research dispositions all converged on cheap-action-no-DEC; Option 1.5 for FB-089 dissolves the "two viable approaches" framing in the original capture). No sync-manifest change. Active feedback.md entries (FB-078/081/086/088/089) replaced with PROMOTED stubs pointing to archive + workspace research docs. Also: **FB-082 + FB-083 bundled** for DEC opening (research complete; matrix populated; user elected to open DEC for the freshness-mechanism choice). All 6 research-gated items from the 2026-05-24 walk-through dispatched in parallel; 5 of 6 shipped here, 1 (FB-082+083) proceeds to DEC.

- **v4.7.3 (2026-05-24)** — `/health-check` triage pass on `template-maintenance/feedback.md` shipped FB-084 + FB-087 as bundled cheap actions (FB-006 / FB-074 multi-cheap-action-ship precedent). **FB-084** — new `## Pre-Retirement Engine-Consumer Audit` section in `.claude/rules/feature-retirement.md` between "When to Use This Workflow" and "Procedure" (4-pattern grep checklist: snake_case + CamelCase derivatives + shortened forms + string literals; ~12 lines). Source: styler 2026-05-22 session (FR-014 / T714 — snake_case-only grep concluded "no engine consumer" for `price_quality_philosophy`; actual engine consumed via `PriceQualityPhilosophy` type + `RankerSignals.philosophy` shortened field + `PHILOSOPHY_WEIGHTS` constant; retirement landed and would have silently degraded runtime). **FB-087** — new `## MCP and Result-Size Constraints` section in `.claude/rules/agents.md` between "## MCP and Parallel Execution" and "## Tool Preferences" (~8 lines). Documents `browser_snapshot` token-budget truncation on long-scroll pages (~10K+ char DOM) and the `browser_evaluate`-with-targeted-queries workaround. Source: styler 2026-05-21 session (105K+ char snapshots on `/style` page). Also: FB-085 marked `deferred + signal-gated (2nd-project)` — proposed `agents.md` behavioral rule has fragile enforcement scope (verify-agent subagent sandbox limits Playwright access). FB-086 marked `research-light-candidate + ready` — git diff cross-check is mechanical but friction-kind schema + timing scope need `/research`. All four FB entries from the 2026-05-24 `/health-check` Part 7 aggregation now triaged. **Captures from this triage pass:** 0 (the pass resolved the four prior captures; no new signals surfaced). **Files touched:** `.claude/rules/feature-retirement.md`, `.claude/rules/agents.md`, `.claude/version.json`, `.claude/dashboard.md` (META `template_version` bumped 4.7.2 → 4.7.3 to match version.json), `template-maintenance/feedback.md` (FB-084-087 status/disposition lines), root `CLAUDE.md` (this entry + Active Follow-ups additions for FB-084 / FB-085 / FB-086). No DEC; no sync-manifest change (both rule edits are content-only on existing sync-category files).

- **v4.11.1 (2026-05-27)** — **Command-table doc reconciliation** (PATCH, no FB — surfaced while addressing a Claude Code "Large CLAUDE.md" performance warning on the root template-maintenance file, same session as the ship-log extraction noted in this file's header). Two user-facing command references had drifted from the actual `.claude/commands/*.md` set (14 commands): `.claude/CLAUDE.md § "Environment Commands"` was missing `/diagnose` + `/zoom-out` + `/audit-coherence` + `/audit-ui`; `.claude/README.md § "Commands"` was additionally missing `/grill` + `/shakedown`. Both tables now enumerate all 14 commands. Root cause: the two help-me-think commands (`/diagnose` v4.4.0, `/zoom-out` v4.3.0) shipped without a CLAUDE.md/README table row, and the audit family (`/audit-coherence` + `/audit-ui`, v3.6.0–v3.12.0) plus the Wave-1/2 commands (`/grill`, `/shakedown`) were never added to the README table. `/audit-coherence` + `/audit-ui` listed with a "usually dispatched by `/health-check`" note (both have direct `triage` modes). **Files touched:** `.claude/CLAUDE.md` (sync — 4 rows added), `.claude/README.md` (customize — 6 rows added), `.claude/version.json` (4.11.0 → 4.11.1), `.claude/dashboard.md` (META `template_version` bump). PATCH per SemVer (documentation completeness; no new commands, no behavior change — consistent with v4.7.2 dashboard-refresh precedent). No DEC; no sync-manifest change.

- **v4.12.0 (2026-05-27)** — **DEC-020: concluded the DEC-007 Skills/reference duplication trial via Option A (retire the 3 Skills, keep the reference docs).** The ~40-day open-ended trial (DEC-007 Option B, 2026-04-17) had no exit criterion and had drifted — the decomposition SKILL led its reference by 52 lines because the FB-073/v4.5.0 `Test-Protocol Runtime Constraints` section was added to the SKILL only. Surfaced from the v4.11.0 "Large CLAUDE.md" session; routed through `/research` → `decisions/decision-020-conclude-skills-reference-trial.md` (+ archive `decisions/.archive/decision-020-research-2026-05-27.md`). **Research basis:** Skill auto-fire is documented-as-unreliable and unobservable in-session (the only precise signal is opt-in OTel `claude_code.skill_activated` / `invocation_trigger: claude-proactive`, captured nowhere in this repo), whereas the orchestrator path-cites the reference docs (observed value) — so on principle the proven-value location is the refs; the evidence-first posture (DEC-018) argues against retiring the proven location for the unproven one. **What shipped:** (a) migrated the `Test-Protocol Runtime Constraints` section into `.claude/support/reference/decomposition.md` (the only real drift; spec-checklist + dashboard drift was whitespace-only); (b) deleted `.claude/skills/` (3 SKILLs), guarded on the migration being present; (c) removed the 3 `<!-- mirror -->` comments; (d) re-pointed the 2 live SKILL-path citations — `support/reference/task-schema.md` + `rules/dashboard.md` — to the reference docs; (e) cleaned ownership lists (`README.md` Skills section + Essential-Files + Where-to-Find rows; `rules/agents.md`; `support/reference/extension-hooks.md`) + `sync-manifest.json` (dropped `.claude/skills/*/SKILL.md`) + the `health-check.md` illustrative sync snippet; (f) `DEC-020` → `implemented`, `DEC-007` annotated with the conclusion. **Files touched (11):** the 3 reference docs, `sync-manifest.json`, `README.md`, `rules/agents.md`, `rules/dashboard.md`, `support/reference/{task-schema,extension-hooks}.md`, `commands/health-check.md`, `version.json`, `dashboard.md` (META) + the 2 decision records + this log. MINOR per SemVer (feature retirement / significant behavior change; non-breaking — nothing referenced the Skills functionally). Also captured **FB-094** (`claude-code-authoring.md` skill-listing-budget drift, surfaced during the research) in `template-maintenance/feedback.md`.

- **v4.12.1 (2026-05-27)** — **FB-094 promoted + FB-091/FB-092 declined** during a `/feedback review` walk-through of `template-maintenance/feedback.md` (FB-090/v4.10.1 same-session-capture→ship precedent). **FB-094** (capability-doc drift, surfaced during DEC-020): rewrote `.claude/support/reference/claude-code-authoring.md § "Skill listing budget"` to separate the **dynamic total** listing budget (~1% of the model's context window; raisable via `skillListingBudgetFraction` setting / `SLASH_COMMAND_TOOL_CHAR_BUDGET` env var; least-invoked descriptions dropped first on overflow, names always kept) from the **per-entry 1,536-char cap** (`description`+`when_to_use`; configurable via `maxSkillDescriptionChars`) — the old heading/body conflated the two. Added a `/doctor` (overflow + which-skills-affected) + `/skills` (visibility menu) observability line. **Verified-then-fixed** per the user-chosen rigorous path: points 1+2 confirmed against `code.claude.com/docs/en/skills` (2026-05-27); point 3's `/context`-as-load-inspector claim **dropped** as unverified (the page documents `/skills` as a visibility menu, not a presence inspector, and doesn't mention `/context`). Footer `Last verified` → 2026-05-27 + `template_version` → 4.12.1. This is the DEC-017 (v4.9.0) capability-doc freshness case the `/health-check` Part 2d `[V]` lens was built to catch, actioned early. **FB-091 + FB-092** (sibling single-source orchestrator-bash-hygiene signals from the 2026-05-25 insights report) **declined**: FB-091 (guard speculative bash probes) — common form already covered by the Tool Preferences `Edit`-not-bash-chain rule; residual too thin + unreproduced. FB-092 (absolute paths / "don't assume CWD persists") — core premise contradicted by the Bash harness contract (CWD *does* persist; only shell state doesn't), absolute-path half already implied; only the `>`-vs-`>>` session-log residual was concrete, too thin for a rule. Both archived `closed` (re-open on a real session-export reproduction). **Files touched:** `.claude/support/reference/claude-code-authoring.md` (section + footer), `.claude/version.json` (4.12.0 → 4.12.1), `.claude/dashboard.md` (META bump), `template-maintenance/feedback.md` (3 stubs: FB-091/092 closed, FB-094 promoted), `template-maintenance/feedback-archive.md` (3 full entries). PATCH per SemVer (mechanical doc correction + feedback triage; no behavior change, no new surface). No DEC; no sync-manifest change (`claude-code-authoring.md` already in `sync`).

- **v4.13.0 (2026-06-10)** — **FB-085 promoted: `/diagnose` visual recipe shipped** (2nd-project gate overridden by user decision 2026-06-10). New `## Visual / browser-rendering bugs` section in `.claude/commands/diagnose.md` (between Phase 6 and Out of scope; ~20 lines) implementing the locked design from the 2026-05-24 `/visual-verify` grill verbatim: orchestrator-level browser loop (subagent MCP-inheritance + single-session constraints honored; ToolSearch load note; dev-server + respect-prior-kills note); contract = falsifiable assertions on measured values (geometry + computed style incl. sampled interaction states), pixel-diffs/golden images excluded; `browser_evaluate` targeted-query measurement with `browser_take_screenshot` demoted to reporting-only (FB-087); N=3 configurable loop, one falsifiable hypothesis per iteration with a pre-computed predicted value-effect (absorbs the 2026-05-20 signal-queue "math-check before commit-to-pixels" item), early-exit when out of distinct hypotheses; non-convergence surfaces unmet contract + hypothesis→prediction→result trace + before/after screenshots, never silent; persistence conditional on a correct-seam test harness (Phase 5 logic). **Gate-override rationale** (recorded in FB-085 archive entry + root `ship-plan-1-evidence-based-verification.md § S1`): the 2nd-project gate predated the grill that fixed cost (~20 lines, zero new surface, trivially reversible); within-styler evidence volume grew past the gate's intent (62/391 session-export friction notes verification-themed; T731 verified-pass-but-500; UI-drift dominant theme in styler's archived feedback; 806 verifications with zero terminal fails). General-merit half (Phase 3 outcome-not-mechanism) had shipped v4.10.2. **Also this session: FB-011 Family C trigger declared MET** on substituted evidence (4b channel structurally unsampled — `/health-check` ~7 mentions across the 47-session corpus; substituted standard = recurrent post-fix dashboard regressions, FB-015→038→080→090 + v4.7.2; amendment recorded in `template-maintenance/scripts-candidates.md § Family C`); scope = PoC only (Tasks-by-phase render, byte-identical re-render test), full port gates on PoC. **Files touched:** `.claude/commands/diagnose.md` (recipe), `.claude/version.json` (4.12.1 → 4.13.0), `.claude/dashboard.md` (META bump), new `tests/scenarios/32-diagnose-visual-recipe.md` (non-convergence + arbiter-discipline + orchestrator-level traces), `template-maintenance/feedback.md` (FB-085 → PROMOTED stub; math-check signal-queue item → absorbed), `template-maintenance/feedback-archive.md` (FB-085 full entry), `template-maintenance/scripts-candidates.md` (4 Family C edits), root `CLAUDE.md` (Active Follow-ups: FB-085 bullet → promoted note; FB-011 line updated). MINOR per SemVer (new behavior surface in an existing command; no breaking change). No DEC (the fold fails the hard-to-reverse criterion, per FB-085's recorded reasoning). No sync-manifest change (`.claude/commands/*.md` glob auto-covers).

- **v4.13.1 (2026-06-10)** — **Pointer-rot + consistency sweep** (PATCH, no FB — Plan 2 P1 from the 2026-06-10 cross-repo analysis; staged in root `ship-plan-2-prose-diet-and-mechanization.md § P1`, temporary working file). Four fix groups: **(1) Stale line-number cross-refs** — `rules/agents.md § Dispatch Convention` cited work.md "line ~605/~688" + research.md "line ~74" (~150 lines stale); replaced with section-name anchors (§ "If Verifying (Per-Task)", § "If Verifying (Phase-Level)", § "Step 3: Spawn Research Agent") + codified "section names, not line numbers" for cross-file refs. Repo-wide grep confirmed this was the only line-ref instance. **(2) Phantom citation** — `commands/audit-coherence.md` path-drift lens used `.claude/support/reference/audit-template-pattern.md` (a file that has never existed in the repo) as its example of "template file legitimately absent in a project"; replaced with the real `audit-fix-workflow.md`. **(3) Model-pin single-sourcing** — `.claude/CLAUDE.md § Model Requirement` is now the declared canonical source for BOTH the design pin (Opus 4.7) and the `Task` dispatch value (`model: "opus[1m]"`), with the alias-vs-pin relationship documented honestly (the alias tracks the latest Opus; the pin records the tested version; divergence handling = deliberate explicit-ID pin, not silent drift). Nine restatement sites now cite it: work.md (prose + 2 Task blocks via citation comments), research.md Task block, the 3 agent-file headers (hardcoded "Opus 4.7 / always set opus[1m]" dual statement removed entirely), `rules/agents.md § Model Requirement`, `parallel-execution.md § 3` (which falsely claimed the alias "ensures" Opus 4.7), and `shared-definitions.md` difficulty-calibration note (pin pointer added). **No dispatched value changed** — this documents de-facto behavior (the alias governs at dispatch; the pin records design target) and kills the silent contradiction. **(4) `/status` icon vocabulary** — `commands/status.md § Status Icons` annotated as display-layer labels (✅ explicitly mapped to canonical task status "Finished"; ⚠️/🔴/🚀 identified as timeline-health annotations computed from dates, not statuses; canonical 8 unaffected). **Flagged, not edited (out of PATCH scope):** `claude-code-authoring.md` § Tool & Dispatch Surface states the Agent tool's `model` param exposes only `sonnet | opus | haiku` — the live harness (observed this session) also exposes `fable`; route through `/health-check` Part 2d `[V]` verification per DEC-017 discipline before editing the capability doc. Also left as-is: `automation.md`'s CLI example `--model claude-opus-4-7` (copy-paste example; explicit ID is appropriate there). **Files touched (13):** `rules/agents.md` (2 edits), `commands/audit-coherence.md`, `.claude/CLAUDE.md`, `commands/work.md` (3 sites), `commands/research.md`, `agents/implement-agent.md`, `agents/verify-agent.md`, `agents/research-agent.md`, `commands/status.md` (2 edits), `support/reference/parallel-execution.md`, `support/reference/shared-definitions.md`, `version.json` (4.13.0 → 4.13.1), `dashboard.md` (META bump). PATCH per SemVer (consistency/doc corrections; zero behavior change). No DEC; no sync-manifest change (all files already in `sync`).
