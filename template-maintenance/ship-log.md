# Template Ship Log

Version-by-version changelog of template-maintenance ships. Extracted from root `CLAUDE.md § Active Follow-ups` on 2026-05-27 to keep the always-loaded `CLAUDE.md` under Claude Code's size budget (the changelog alone was 46k of the file's 60k chars).

Each entry records: version + date, what shipped, the FB/DEC it resolves, files touched, and SemVer rationale. Entries are roughly version-ordered — a few interleave out of strict order where ships overlapped in time (preserved as-is from the original log). **Append new ships here**, not in `CLAUDE.md`.

This file does **not** ship to projects — it's template-maintenance history. See also: `template-maintenance/feedback.md` (active FB queue), `decisions/` (template decision records), and root `CLAUDE.md § Active Follow-ups` (forward-looking deferred work).

---

- **v3.4.0 (2026-05-13)** — DEC-010 Option C: `partial_completion` envelope for usage-limit graceful resume (`decisions/decision-010-partial-completion-envelope.md`). Promoted FB-049.
- **v3.5.0 (2026-05-13)** — DEC-011 Option ABp: hybrid A+B + `.pending-markers.jsonl` transient buffer for Track 1 pipeline reliability (`decisions/decision-011-track1-pipeline-execution.md`). Promoted FB-057.
- **v3.6.0 → v3.12.0 (2026-05-15, 7 commits)** — Audit family: 6.5 of 7 stages from `template-maintenance/audit-command-family-proposal.md`. Dashboard slim (META whitelist + Recent Activity cap); friction register at `.claude/support/friction.jsonl`; commands `/audit-coherence` (6 lenses) + `/audit-ui` (7 lenses + mobile) dispatched by `/health-check` Part 8; persistent `🔍 Audit Findings` dashboard section with `[Promote to FB] / [Dismiss]`; `[Fix it]` inline-apply for `bundle-eligible` kind only per DEC-013 Option C (`decisions/decision-013-audit-fix-it-autonomy-boundary.md`).
- **v3.14.0 → v3.14.2 (2026-05-16)** — FB-003 feature-retirement workflow rule promoted; FB-062 + FB-063 cheap actions shipped; FB-060 Phases 1 + 5 (Cross-Project Capture Protocol in `.claude/rules/agents.md` + new `.claude/support/reference/extension-hooks.md`).
- **v3.15.0 (2026-05-16)** — DEC-014 Option F: `.claude/.sync-state.json` sidecar + `/health-check` Part 5 2-condition algorithm refinement (`decisions/decision-014-sync-state-and-file-ownership-categories.md`). Closes FB-059; partially closes FB-060.
- **v3.16.0 (2026-05-16)** — FB-065 + FB-066 promoted: 5th heuristic on FB-058's Pre-Pass table (decomposition-time enum-extension ripple inference) + production-consumption check in verify-agent Step T5 (catches class-export integration gaps).
- **v3.17.0 (2026-05-16)** — FB-064 promoted: Test-Harness Awareness section added to decomposition.md + SKILL.md (mirror). Detects runtime-shaped tasks (`interaction_hint: cli_direct`, runtime-surface globs, runtime/UI `test_protocol`); scans for conventional harness directories with root `./CLAUDE.md` opt-in for custom paths; proposes scenario-authoring `_h` subtask when harness exists. Bridged from echothread `FB-009`.
- **v3.17.1 (2026-05-16)** — FB-006 PATCH iteration 1: dropped per-finding `[Promote to FB] / [Dismiss]` inline text from dashboard audit-findings render (closes FB-006 sub-issue 3 "dead UI"). `[Fix it]` retained for bundle-eligible (no keyword alternative); non-bundle-eligible kinds show italicized kind annotation only. `dashboard-regeneration.md` § "Audit Findings sub-section" + `dashboard-style/SKILL.md` mirror + `audit-fix-workflow.md` example updated; new "How to act on findings" sub-section documents promote (tick + bulk CLI) and dismiss (natural-language) invocation patterns. Plain-English description (sub-issue 4) + `/audit-coherence triage` (sub-issues 1+2) + dual-checkbox column deferred to future MINOR iterations.
- **v3.18.0 (2026-05-16)** — FB-006 MINOR iteration 2: added `description` field to `digest.json items[]` (Component 2 schema) for plain-English at-a-glance dashboard triage (closes FB-006 sub-issue 4 "opacity at decision moment"). Synthesizer prompts in `commands/audit-coherence.md` + `commands/audit-ui.md` add Algorithm step 4 instruction: complete sentence, period-terminated, ~80-140 chars, names the affected artifact, distinct from `title` (which stays as terse cluster identifier for findings.md headers + FB titles on promote). Render rules in `dashboard-regeneration.md` + `dashboard-style/SKILL.md` mirror use `{description ?? title}` for backward-compat with pre-v3.18.0 digest.json files. `audit-command-family-proposal.md` § Component 2 schema example shows new field; `audit-fix-workflow.md` example block updated with plain-English descriptions.
- **v3.19.0 (2026-05-16)** — FB-006 MINOR iteration 3: `/audit-coherence triage [audit-ts]` + `/audit-ui triage [audit-ts]` interactive walkers shipped (closes FB-006 sub-issues 1 "courier pattern" + 2 "audit-name memory burden"; all four FB-006 sub-issues now closed). `audit-ts` defaults to `latest` (newest `coherence-*`/`ui-*` audit dir by `ran_at`) — user never types the audit name. Walker iterates pending non-dismissed findings (`status: pending` AND id NOT in sidecar `dismissed_ids[]`), renders each as `[i/N] {C-ID} ({kind})` + `description ?? title` (pre-v3.18.0 fallback) + `Files to touch` + `Source anchors` + kind-conditional `Actions:` prompt, reads user response (single-letter `F/P/D/S/Q` or natural-language verbs with reason inline), dispatches to canonical per-action mechanics in `audit-fix-workflow.md` (no divergence). Auto-advance between findings; `[Q]uit` available at every prompt; `Ctrl+C` mid-walk is safe (completed actions stay committed, remainder stays pending). Cross-refs: `commands/audit-coherence.md § "Triage mode"` (H2, full algorithm) + `commands/audit-ui.md § "Triage mode"` (H3 under Workflow, parallel-by-substitution with C→F prefix); `support/reference/audit-fix-workflow.md` "Preferred entry point for N pending findings" section at top of Action protocol; `support/reference/dashboard-regeneration.md` + `skills/dashboard-style/SKILL.md` mirror "How to act on findings" sub-section adds "Preferred for multiple findings" bullet. Dual-checkbox promote-vs-dismiss column closed as obsoleted by triage walker.
- **v4.0.0 (2026-05-16)** — **MAJOR**: DEC-016 Option D promoted — Claude direct-edit guardrail on spec/decision/vision files via composite rule + permission-layer enforcement (closes FB-007). **Inflection point**: first template-wide `permissions.ask` shipping in template-owned `.claude/settings.json` — the convention now is "base `permissions.allow` AND base `permissions.ask`" for the file. Six `permissions.ask` globs added (Edit + Write × `.claude/spec_v*.md`, `.claude/support/decisions/decision-*.md`, `.claude/vision/**/*.md`) — symmetric with DEC-013's audit-system protection. New `## Direct edits to spec, decision, and vision files (DEC-016)` section in `rules/spec-workflow.md` replaces the "direct edits always safe" line with explicit routing rule (substantive text edits MUST route through `/iterate` or analogous per category; infrastructure operations like archiving/version transitions/frontmatter remain autonomous). `.claude/CLAUDE.md` Critical Invariants updated (two invariants: settings layering now mentions both base sets; new invariant points to the spec-workflow section). Cross-reference updates: `sync-manifest.json` notes, `.claude/README.md` settings description, `support/reference/extension-hooks.md` settings-layering reference, `commands/health-check.md` Part 5c (allows `permissions.ask` as template-owned key; drift comparison expanded to both arrays; rationale prose updated). Decision record at `decisions/decision-016-spec-file-edit-guardrail.md` (status: implemented; 7 implementation_anchors). Research archive at `decisions/.archive/decision-016-research-2026-05-16.md`. Per-amendment user cost: one click per `/iterate apply` (or `/research`) session at the first Edit; platform-native "Yes don't ask again" persists for session.
- **v4.1.0 (2026-05-20)** — FB-071 promoted: `disable-model-invocation: true` frontmatter applied to all 5 strong-candidate commands (`/breakdown`, `/research`, `/iterate`, `/work`, `/feedback`). Blocks Claude's autonomous invocation via the `Skill` tool while preserving user-typed slash invocation. **Live skills-list verification immediate** (during the ship session): the model-invocable template-commands set shrank from 10 → 5 — the 5 strong candidates were removed, while the 5 weak candidates (`/status`, `/health-check`, `/review`, `/audit-coherence`, `/audit-ui`) remained ambient-invokable as designed. New `## Command Invocation Gates` section in `.claude/rules/agents.md` (between `## Behavioral Rules` and `## Cross-Project Capture Protocol`) documents convention + selection criteria + sub-mode coupling trade-off (multi-mode files gate as a whole — read-only sub-modes like `/work` no-args also lose ambient invocation; user-typed invocation still works; future option to split files if friction surfaces) + defense-in-depth with DEC-005 (auto-mode permission layer) and DEC-016 (spec/decision/vision Edit/Write ask). Medium candidates (`/iterate distill`, `/iterate propose`, `/audit-coherence triage`, `/audit-ui triage`) deferred per FB-071 design — re-evaluate after a few weeks of strong-candidate gating in practice. No new files; no sync-manifest change. Single commit; no DEC.
- **v4.2.0 (2026-05-20)** — FB-068 promoted: new `/grill` command (interview-style interrogation, branch-by-branch decision-tree walking, one question at a time, recommend an answer each) + `./CONTEXT.md` slot (project-owned, lazy-created domain glossary at project root). Adapted from `mattpocock/skills/engineering/grill-with-docs` — preserves the with-docs flow (challenge fuzzy language, sharpen terms, cross-reference code, update CONTEXT.md inline, offer ADRs sparingly with 3 criteria) and integrates with CCE's `/iterate` + `/research` routing + DEC-016 guardrail (no direct writes to spec/decision/vision). Integration points: `./CONTEXT.md` row added to `.claude/CLAUDE.md` Navigation table; `/grill` in Environment Commands; new `## Domain Glossary Awareness` section in `rules/agents.md` (placed after `## Root Cause Over Symptom`, before `## Behavioral Rules`) — codifies implement-agent / verify-agent / maintenance / layer-distinction rules; step 3 added to `rules/spec-workflow.md § Vision Documents` as optional pre-distill enrichment; `audit-coherence.md § "Lens 2 — vocab-drift"` extended to consume CONTEXT.md as canonical naming reference when present. Live skills-list verification: `grill: Grill Command` appeared immediately. All FB-068 explicit out-of-scope items honored: no starter CONTEXT.md shipped (per Pocock's deprecation lesson — placeholders don't get filled); no batch-extract from spec/code; no `CONTEXT-MAP.md` for monorepos; no `/grill` direct writes to `decisions/` (routes to `/research` instead); no glossary fingerprinting; no co-equal source-of-truth machinery. Ship-time deviation from capture: original plan said "wire as `/iterate grill` sub-mode"; shipped as standalone command referenced from spec-workflow.md to avoid bloating `iterate.md` (already gated as a whole under FB-071). `/grill` does NOT carry `disable-model-invocation: true` — the interactive one-question-at-a-time pattern is the safeguard; Pocock's `grill-me` doesn't gate either.
- **v4.3.0 (2026-05-20)** — FB-070 promoted: new `/zoom-out` micro-command (~50 lines, single file). Adapted from `mattpocock/skills/engineering/zoom-out` (Pocock's 7-line skill); domain-genericized for CCE (software, research, procurement, renovation — any spec-driven project). Consumes `./CONTEXT.md` vocabulary when present (FB-068 integration); degrades gracefully when absent. Carries `disable-model-invocation: true` frontmatter on day one per FB-071's convention — `/zoom-out` is a user-asks-for-help signal where autonomous fire would be circular (the model would be invoking it for its own benefit, which doesn't match the help-the-user trigger). Pocock's `/zoom-out` carries the same frontmatter for the same reason. No sync-manifest change (`.claude/commands/*.md` glob auto-covers); no DEC needed. Also: **FB-072 captured** in `template-maintenance/feedback.md` — research-first investigation of command routing as a UX pattern (interpretive vs explicit-arg dispatch + boundary survey across `/iterate` / `/work` / `/research` candidate umbrellas). Trial-gated DEC candidate. Surfaced during the FB-068 ship review: user observed that `/grill` as a standalone command adds UX surface, and proposed `/iterate` become an interpretive router that dispatches sub-purposes from natural language. FB-072 documents the pattern, the survey deliverable, the research questions, and the trial-gate criteria; no implementation until survey + prototype + effectiveness data accumulate.
- **v4.4.0 (2026-05-20)** — FB-069 promoted: new `/diagnose` command (6-phase debugging methodology: feedback loop → reproduce → falsifiable hypotheses → instrument → fix + regression test → cleanup + post-mortem; ~170 lines). Adapted from `mattpocock/skills/engineering/diagnose`. Domain-genericized for CCE — the methodology applies to any "something is wrong, I don't know why" task, not only software bugs. Phase-1 feedback-loop discipline + Phase-3 falsifiable-hypothesis discipline are the structural enforcement mechanisms for `agents.md § "Root Cause Over Symptom"` on multi-turn debugging (the Phase-3 discipline is specifically what prevents the "swap-and-see" pattern that produces symptom-only fixes). Phase-6 architectural-friction handoff routes through CCE's friction register (`design_contradiction` kind) or `/research` for substantial architectural decisions — no `/improve-codebase-architecture` exists yet (FB-067 Wave 2). Cross-references shipped: (a) `agents.md § "Root Cause Over Symptom"` extended with a paragraph routing hard-bug cases through `/diagnose` (mutual reference); (b) `spec-workflow.md § "Workflow Cycle"` extended with a "Bug tasks" paragraph naming `/diagnose` as the preferred route. **Leaves `disable-model-invocation: true` OFF** per the FB-071 selection criteria (three options discussed: gate now / leave open / add to medium candidates; chose leave-open because autonomous-fire-when-stuck is exactly the value — when implement-agent hits a hard bug mid-`/work`, sweeping into structured methodology is the feature, not the foot-gun; can flip to gated later if observed misfires accumulate). No sync-manifest change (`.claude/commands/*.md` glob auto-covers); no DEC needed. Also: **FB-072 candidate list extended** with "Help-me-think family" umbrella (`/zoom-out`, `/grill`, `/diagnose`) — three help-me-think commands now exist; worth surveying whether a single `/help` or `/think` entry point + interpretive dispatch produces better UX than three discrete commands. **Wave 1 complete** — FB-068 + FB-069 + FB-070 + FB-071 all shipped 2026-05-20 across v4.1.0 → v4.4.0.
- **v4.5.0 (2026-05-20)** — FB-073 promoted (Wave 2 start): new `## Test-Protocol Runtime Constraints` section in `.claude/skills/decomposition-heuristics/SKILL.md` (sibling to existing Test-Harness Awareness section). Covers detection patterns for test_protocol steps that exercise runtime behavior not testable under the project's primary phone runtime (Expo Go's offline cold-launch limitation is the canonical example — Expo Go fetches its JS bundle from Metro on every cold-launch, so airplane-mode cold-launch freezes Expo Go itself regardless of in-app cache correctness). Three substitution patterns: background mode (lock → unlock, JS stays in memory), server-only kill (dev server stopped + WiFi on, app force-quit + relaunch — tests fetch-failure → cache-render branch), defer-to-dev-client (post-EAS). Annotation pattern via `constraint` informational field on test_protocol steps; documented as informational only (formal schema field deferred until convention proves itself). Optional project-side `**Primary phone runtime:**` declaration in root `./CLAUDE.md`. Cross-reference added to `.claude/support/reference/task-schema.md § Test Protocol Field` (new `### Runtime Constraints` sub-section pointing at the skill). Source: styler FB-174 bridge (iPhone attestation walkthrough hit the limitation). No DEC; no sync-manifest change.
- **v4.6.0 (2026-05-20)** — FB-074 promoted (bundled three sub-issues, FB-006 precedent): (1) **Canonical decision categories extended** with `ux`, `design`, `ui-ia`, `ui-content` in `.claude/support/reference/decisions.md` (template enum line 22 + Categories table with one-sentence definitions) AND `.claude/commands/health-check.md` Part 3 check 1 (validation enum). Addresses 22/100 styler decisions whose categories fell outside the previous 6-value set; `data` category deferred per FB-074 body's "lower priority" annotation. (2) **Parent-task aggregate-subtask verification exception** added to `.claude/commands/health-check.md` Part 1 check 7 alongside the existing `owner: human` self-attested exception. Pattern: parents with status `"Broken Down"` (non-empty `subtasks`) OR `"Finished"` (non-empty `subtasks` where every subtask is itself `"Finished"` with passing per-task verification) that have `checks.aggregate_subtask_verification: "pass"` are exempt from the 7-key requirement. Pattern-mirror of existing exception; no schema change. (3) **Rules-file soft cap raised 200 → 220** in `.claude/commands/health-check.md` Part 2c. Closes the standing warning on `.claude/rules/feature-retirement.md` (203 lines, genuinely rich procedure — procedure + 5 edge cases + restore path + worked examples). Source: styler FB-192 bridge. No DEC; no sync-manifest change. Also: **FB-077 captured** in `template-maintenance/feedback.md` — auto-mode classifier false-positives surfaced in-session: (a) classifier blocked Edit to `.claude/support/reference/decisions.md` citing DEC-016, but DEC-016 only covers spec/decision-record/vision files — not the reference doc *about* decision format; (b) AskUserQuestion responses don't count as "visible user response authorizing the retry" — classifier requires typed-text. Resolved with explicit typed-text user authorization. FB-077 captured for upstream-Anthropic / DEC-005 follow-up.
- **v4.6.1 (2026-05-20)** — FB-075 cheap action shipped: "Harness reminders about built-in task tools" paragraph added to `.claude/README.md § Known Constraints` documenting that the TaskCreate / TaskUpdate harness-emitted reminder is unavoidable and benign in projects using `.claude/tasks/*.json` (the reminder is harness-emitted, not template-emitted; agents ignore it automatically; noise is visible but benign). Empirically observed in this very session 2026-05-20 during inbox triage — the harness reminder fired multiple times including immediately after the AskUserQuestion confirming FB-075's capture (noted verbatim in FB-075's Source line). Structural fix (per-project opt-out) requires Anthropic-side mechanism and remains upstream-gated; FB-075 stays in `template-maintenance/feedback.md` as `cheap-action-shipped + deferred`. Also: **FB-076 deferred** (formalized) — verify-agent runtime_validation gaps (bundle-boundary breaks + catalog-state-dependent precondition gaps from styler T667). Mitigations (`npm run build` invocation, ESLint client-import rule, live-data cross-reference) require design work less mechanical than FB-066's regex+grep; single-project signal doesn't justify it yet. Re-assess on 2nd project signal or FB-066 downstream telemetry; if escalated, route through `/research`. No DEC; no sync-manifest change.
- **v4.6.3 (2026-05-20)** — FB-077 cheap action shipped: "Auto-mode classifier false-positives" paragraph added to `.claude/README.md § Known Constraints` (sibling to FB-075's TaskCreate harness reminder paragraph). Documents two observed runtime-classifier behaviors that can block legitimate work: (a) **DEC-016 scope over-broad** — edits to `.claude/support/reference/decisions.md` may be blocked citing DEC-016 even though DEC-016 only covers spec/decision-records/vision files; workaround: lead with explicit context-clarifying language ("this is the reference doc, not a decision record"), or user provides typed-text authorization; (b) **AskUserQuestion responses don't count as classifier-bypass authorization** — the classifier requires typed-text "visible user response"; workaround: agents prefer free-text prompts (not AskUserQuestion) when classifier-bypass authorization is the goal. `AskUserQuestion` remains appropriate for collecting preferences and structured decisions. Both behaviors are upstream-Anthropic (classifier lives in the runtime, not the template); structural fixes remain upstream-gated. FB-077 stays in `template-maintenance/feedback.md` as `cheap-action-shipped + deferred`. No DEC; no sync-manifest change.

- **v4.6.2 (2026-05-20)** — FB-011 Family E permanently dropped. 30-day trial window (2026-04-17 → 2026-05-17) closed with no FB-017 regression observed in this template repo or across three downstream projects (echothread, styler, SIREN). FB-017's inlining fix at `commands/work.md` Step 2b + `commands/iterate.md` is sufficient; decision auto-finalization does not need script extraction. `template-maintenance/scripts-candidates.md` updated to reflect Family E closure across four locations (section header + Recommendation + Trial window + Tier 3 § Recommended extraction order + Next action). Root CLAUDE.md FB-011 entry updated — Family E removed from the active re-assess list; C/D remain deferred per observed-need gates. Also: **FB-078 captured** in `template-maintenance/feedback.md` — surfaced 2026-05-20 during the Family E re-assessment as a side-discovery in styler's 2026-05-16 session export `workflow_friction_notes`. `/work` Step 2b post-decision check fires `/iterate` suggestion based on the decision-level `inflection_point` flag alone, ignoring whether the chosen option has spec impact. Two mitigation candidates documented (heuristic vs schema flag). Single-project signal; not promoted yet. Distinct from FB-017's decision-auto-finalization sub-feature — same Step 2b function, different sub-paths. No DEC; no sync-manifest change.

- **v4.6.4 (2026-05-20)** — FB-079 promoted: `/work pause` session-export filename collision fixed. Minute-granularity timestamp (`YYYY-MM-DD-HHMM`) applied to both write sites: (a) `.claude/commands/work.md` line 1057 (orchestrator pause procedure step 5 — instruction text updated to the new pattern with inline FB-079 reference), and (b) `.claude/hooks/pre-compact-handoff.sh` lines 230 + 237 (PreCompact-hook fallback path — new `timestamp = datetime.now(timezone.utc).strftime("%Y-%m-%d-%H%M")` variable; both the workspace export path AND the template-inbox copy use `timestamp` rather than `today`). The existing `today` variable continues to drive task-filtering by `completion_date` AND the `session_date` payload field (which stays YYYY-MM-DD per export schema, separate concern from filename). Direct mirror of SIREN's own build-bundles.py fix pattern (per FB-079 capture). Same-day pauses now produce distinct filenames rather than silently overwriting earlier exports. The `.session-export-*.json` glob already matches the new filename shape; no reader-side change needed (verified — no other write or glob-based read sites in the template). Captured + promoted same session (`/feedback`-style scan-then-ship loop): FB-079 captured 2026-05-20 morning during cross-project signal scan from `interaction-logs/processed/SIREN-task-7.5-session-export-2026-05-18-T1530.json`; shipped 2026-05-20 afternoon in v4.6.4. No DEC; no sync-manifest change (the two affected files are already in `sync` category; diff is content-only). Also: **FB-080 + FB-081 captured** (from same scan) — Dashboard full-regen too heavy for incremental updates (multi-session signal: styler 2026-05-17 + echothread 2026-05-17; two routes: partial-regen mode vs formalize defer-then-regen pattern), and long autonomous batches (3+ implement+verify cycles) lack heartbeat or user-check-in default (single-session signal but tied to concrete observable friction event; two patterns documented: heartbeat between dispatches + ping-mid-batch behavior rule). Both remain `new` in `template-maintenance/feedback.md`; signal-queue note in same file captures six additional weaker signals for next-session triage. Also: **`.claude/support/workspace/router-survey.md`** drafted as FB-072 deliverable 1 — boundary survey of five candidate router umbrellas (`/iterate`, `/work`, `/research`-vs-`/iterate` overlap, `/audit-*`, help-me-think family); net recommendation: prototype interpretive routing on `/iterate` only; defer others. Survey is a workspace draft, not a template ship — FB-072 itself stays `ready` pending user response to the three decision points at the bottom of the survey.

- **v4.7.2 (2026-05-23)** — Dashboard format-example refresh (PATCH, no FB — surfaced via `/health-check` Part 1 format-staleness check). The shipped placeholder dashboard at `.claude/dashboard.md` was last regenerated under template v3.0.0 and had drifted from current `skills/dashboard-style/SKILL.md` rules in two places: (a) META block missing 6 of the 13 whitelisted fields (`spec_version`, `spec_status`, `decision_count`, `decisions_approved`, `decisions_superseded`, `decisions_partially_superseded`) plus `template_version` stale at 3.0.0; (b) Decisions table Selected column not following the "always link, Decided shows option-name as link text, Pending shows 'Pending' as link text" rule from `dashboard-style/SKILL.md § "Section Display Rules"` — DEC-001's selected option was plain text and DEC-002 used "Review options" as link text instead of "Pending". Both fixed. META field order now matches the SKILL.md Step 4 canonical order (task_count before task_hash; spec_version + spec_status after task_hash; decision counts at the end). No body content changes — the renovation narrative dates (Jan 2026) stayed intact since they form a coherent fictional mid-execution snapshot. Audit Findings sub-section + Recent Activity sub-section deliberately not added — both auto-render only when data exists and the renovation example has neither (auditing the rules would have meant inventing fake findings, which teaches the format but pollutes the example narrative). No sync-manifest change (`.claude/dashboard.md` is in `ignore` category — doesn't sync to downstream projects post-clone, but does ship on initial clone, so the refresh propagates to all new projects from this point). Single commit; no DEC. Also: **FB-083 captured** in `template-maintenance/feedback.md` — capability grounding for spec/agent/model/skill design + freshness mechanism for `.claude/support/reference/claude-code-capabilities.md` (or merged-doc alternative). Research-gated; paired with FB-082 (the two together suggest a broader "Claude Code authoring hazards" template surface). Source: flirty-gym 2026-05-22 session export (now in `interaction-logs/processed/`). Likely DEC candidate after research — the freshness mechanism (WebFetch sync vs cadence lens vs version-pinned + manual vs footer) is the substantive decision; the capability reference content is more mechanical once the mechanism is chosen.

- **v4.7.0 (2026-05-20)** — FB-080 promoted: dashboard regen cost vs deferral pattern resolved via Route C1 (hybrid: targeted-edit pattern + sidecar sentinel). Selected over Route A (section-fingerprints in META — substantial, grows the "strict 13" whitelist, risk of dirty-tracking bugs) and Route B (defer-everything-to-session-boundary — cheap but throws away mid-session value Erik is already getting from informal targeted edits). Research analysis in `.claude/support/workspace/fb-080-research.md`. **What shipped (six files):** (a) `.claude/skills/dashboard-style/SKILL.md` — new `pending_full_regen: String | null` field added to sidecar schema (JSON example + field-definitions table) + new top-level section "Targeted Edits (mid-session lite path)" inserted between "When to Regenerate" and "Regeneration Steps" with 10-pattern decision table (4 targeted-eligible: single phase decision, single task→Action Required, META timestamp refresh after status flip, format-staleness fix touching only META; 6 require-full-regen: decomposition complete, parallel batch end, `/work complete`, session boundaries, multi-task changes ≥3, spec version transitions), 4-step procedure (`Edit` affected section → recompute `task_hash` + refresh `generated` timestamp in META → set sentinel in sidecar → skip 8-step regen procedure), cycle-and-clearing rules, rationale paragraph; brief pointer added after Tier 2 parallel-mode note. (b) `.claude/support/reference/dashboard-regeneration.md` — parallel mirror of all SKILL.md changes (same insertion points, identical content). (c) `.claude/commands/work.md § "Step 1a: Dashboard Freshness Check"` — new paragraph instructing orchestrator to read `pending_full_regen` field and trigger full regen if non-null. (d) `.claude/support/reference/drift-reconciliation.md § "Dashboard Freshness Check"` — added check 5 (sidecar sentinel) to the freshness-check procedure; "Why this matters" paragraph extended to mention three staleness modes (content / format / drift). (e) `.claude/rules/dashboard.md § "Regeneration Strategy"` — one-paragraph pointer to the new SKILL.md section. (f) `.claude/version.json` — 4.6.4 → 4.7.0. **Behavior change:** before, Tier 1 triggers fired full regen and orchestrators informally deferred + did targeted edits anyway (observed twice-repeated in styler + echothread session exports); after, sanctioned targeted-edit path is documented with explicit decision table + sentinel-backed guarantee that Step 1a will catch any accumulated drift. **Compatibility:** missing `pending_full_regen` field treated as `null` (back-compat for sidecars created before this convention). No DEC (route selection bundled into ship per research-light deliverable). No sync-manifest change (all six files already in `sync` category). MINOR per SemVer policy (new sanctioned behavior surface; no breaking change). Route A remains available as future iteration if Route C1 produces residual friction.

- **v4.10.0 (2026-05-24)** — `/grill` no-args triage flow added (single-session ship via in-conversation grill of the design itself; design walked end-to-end with explicit recommendations + counter-evidence at each branch). `/grill` is now two-mode: explicit-topic flow (`/grill {topic}` or `/grill {vision-file}`) drills directly per existing Process content; no-args flow (`/grill`) scans project state (default scope: spec + decisions + recent FB items + `./CONTEXT.md` when present; widens to code if those don't surface enough), surfaces 2-4 candidate areas via AskUserQuestion with "(Recommended)" appended to the suggested starter (first in list per the tool's convention), then drops into the explicit-topic flow at the picked area. Edges documented: zero good candidates → suggest explicit-topic invocation; multiple in sequence → model references the original triage list if user wants to drill another. **Files touched (5):** `.claude/commands/grill.md` (lede + Usage no-args line + 5th When-to-Use item + Process restructured into two sub-flows), `.claude/CLAUDE.md` (Environment Commands `/grill` row), `.claude/version.json` (4.9.0 → 4.10.0), `.claude/dashboard.md` (META bump), root `CLAUDE.md` (this entry). MINOR per SemVer (new behavior surface for the no-args case; no breaking change to explicit-topic invocation). No DEC (design decisions made in-session via `/grill`'s own discipline: γ two modes, Y format-only spec, P AskUserQuestion structured pick, II default scope listed with widen-to-code escape hatch). No sync-manifest change (`.claude/commands/*.md` glob auto-covers). Ship-process note: 3-commit per-ship surgery (v4.8.0 + v4.9.0 + v4.10.0) since prior session ships were uncommitted in working tree; backup+edit+add+restore mechanism used in lieu of `git add --patch` (interactive, unsupported in headless conversations).

- **v4.10.1 (2026-05-24)** — FB-090 cheap action shipped (same-session capture → promotion → ship during a `/feedback review` walk-through; FB-084 / FB-087 precedent). Made the dashboard Recent Activity 7-entry cap **enforcement non-discretionary** and added cap-trim as a sanctioned targeted-edit pattern, now that FB-080 (v4.7.0) made single-section trims cheap. **Files touched:** two edits each to `.claude/skills/dashboard-style/SKILL.md` + its mirror `.claude/support/reference/dashboard-regeneration.md` — (1) new "Recent Activity cap-trim (drop entries above the 7-entry cap)" row in the "Targeted Edits (mid-session lite path)" decision table; (2) new "Cap enforcement is non-discretionary (FB-090)" sub-bullet on the Recent Activity Section Display Rule, tying cap-trim to any edit that touches the section (full regen OR targeted edit) and removing the regen-cost excuse. Plus `.claude/version.json` (4.10.0 → 4.10.1) + `.claude/dashboard.md` META bump. PATCH per SemVer (tightens an existing cap + the existing FB-080 targeted-edit table; no breaking change — consistent with v4.7.3's cheap-action rule additions). No DEC. No sync-manifest change (both content files already in `sync`). FB-090 was promoted from the 2026-05-20 signal queue once its blocker (FB-080) had shipped; full entry archived in `template-maintenance/feedback-archive.md`, active stub left in `feedback.md`. Same `/feedback review` pass also **kept** the "math-check before commit-to-pixels" signal-queue item queued (single-project + niche; held for a 2nd-project signal).

- **v4.10.2 (2026-05-25)** — `/diagnose` Phase 3 sharpen: **assert the outcome, not the mechanism**. New paragraph in `.claude/commands/diagnose.md § "Phase 3 — Hypothesise"` (between the falsifiable-prediction format and the "show the ranked list" checkpoint) requiring each hypothesis's prediction to be phrased as an observable end-state ("after X, measured value Y within tolerance T"), never as a mechanism claim ("because Z re-evaluates mid-operation"). Rationale: a mechanism-shaped prediction can be rubber-stamped by a reviewer who shares the same wrong model — writer + reviewer agree and both are wrong (the FB-085 writer/reviewer shared-premise blindspot); an outcome-shaped prediction stays falsifiable regardless of mental model and fails on its own when the mechanism belief was wrong (would have caught styler's T697 anchor-scroll case regardless of domain). **General-merit** — applies to all bug classes, not just UI. **Provenance:** outcome of the `/visual-verify` grill (2026-05-24, `template-maintenance/visual-verify-vision.md`), which concluded the proposed command should **fold into `/diagnose`** rather than add new surface (FB-072/DEC-018 command-surface discipline). This Phase 3 sharpen is the one general-merit piece extracted now; the UI-specific browser-loop recipe stays captured in FB-085 (deferred, signal-gated), ready to ship on a 2nd-project signal. **Files touched:** `.claude/commands/diagnose.md` (Phase 3 paragraph) + `.claude/version.json` (4.10.1 → 4.10.2) + `.claude/dashboard.md` (META `template_version` bump); plus capture-only edits to `template-maintenance/feedback.md` (FB-085 § "Resolved design") + `template-maintenance/visual-verify-vision.md` (resolution note). PATCH per SemVer (wording/discipline sharpen to an existing command; no new surface, no breaking change — consistent with v4.10.1 / v4.7.3 cheap-action precedent). No DEC (the grill confirmed the fold fails the hard-to-reverse criterion). No sync-manifest change (`.claude/commands/*.md` glob auto-covers).

- **v4.11.0 (2026-05-27)** — **DEC-019: new `/shakedown` command** — acceptance-by-example capability-boundary mapping. Surfaced from two styler transcripts (FB-093), grilled as a design (`template-maintenance/shakedown-workflow-vision.md § Resolution`), formalized via `/research` into `decisions/decision-019-shakedown-command.md` (Option A: **standalone**, over fold-into-`/grill` / pipeline / pattern-doc). **What it is:** the mirror of `/grill` — grill drills *down* to sharpen what you *mean*; `/shakedown` sweeps *across* to test the built/envisioned system against real-use examples (typical + edge), grounding each against the actual system and verdicting (✓/⚠/✗/❓) into a dated, snapshot-anchored corpus that is triple-duty (gap analysis + acceptance probes + forward-direction map). **Phase 0 is a required directed grill** (cross-refs `grill.md § Process`) that calibrates the lens — what the session tests for + the relevant parts of the system + a non-thin lens — before any example; that *requirement* is what ruled out the lightweight-sub-mode-of-grill framing (can't fold X into Y when X requires Y as a precondition). Then the probe loop: restate → break down → **ground against the actual system with a precise *why*** → verdict → write immediately; maintain the model between examples; proactively steer; stop at saturation; route ⚠ → `/iterate` or `/research` (proposes, never writes spec — DEC-016). Domain-agnostic (lens derived per-project at Phase 0; styler's mechanism/bite/direction/when/unless is illustration, not baked in). Ungated (like `/grill`/`/diagnose` — interactivity is the safeguard). **Files (8):** new `.claude/commands/shakedown.md`; new `.claude/support/shakedowns/README.md` (artifact-home convention — dir + README ship, contents project-generated, **no template `.gitignore` entry, mirroring `support/audits/`**); `.claude/sync-manifest.json` (README registered; `commands/*.md` glob auto-covers the command); `.claude/CLAUDE.md` (Navigation + Environment Commands rows); `.claude/rules/spec-workflow.md § "Workflow Cycle"` (working-backward pointer); `.claude/commands/grill.md § "Where it fits"` (inverse-sibling cross-ref); `.claude/version.json` (4.10.2 → 4.11.0); `.claude/dashboard.md` (META bump). MINOR per SemVer (new command surface; no breaking change). **DEC-019** (status `implemented`) carries the `/grill`-vs-`/shakedown` boundary + n=1-evidence honesty (built now because net-new + ungated + reversible; Re-open/down-grade condition = fold into `/grill` à la `/visual-verify` if it underperforms). **Feasibility check** (against `claude-code-authoring.md`, DEC-017): buildable as a main-conversation command (so it writes its own doc + runs the loop outside the subagent `.claude/`-write sandbox); body written as standing patterns, compaction-safe. FB-093 → PROMOTED. **Process note:** the full chain ran in one session — FB capture → `/grill` the design → `/research` → DEC-019 → build+ship — the first end-to-end *grill-the-design → research → ship-a-new-command* run (vs `/visual-verify`'s grill-concludes-fold).

- **v4.9.0 (2026-05-24)** — DEC-017 Option B shipped: capability authoring doc + `/health-check` freshness lens. Decision record at `decisions/decision-017-capability-authoring-doc-freshness.md`; research at `.claude/support/workspace/fb-082-083-research.md`. **What shipped (10 files):** (a) new `.claude/support/reference/claude-code-authoring.md` — 5 sections (YAML Frontmatter Hazards, Skill Frontmatter Scope, Subagent Boundaries, Tool & Dispatch Surface, MCP Constraints) + footer with `Last verified against Claude Code docs @ YYYY-MM-DD; against template_version: X.Y.Z`. Consolidates facts not surfaced anywhere (turn-scoped `model:`/`effort:` frontmatter, YAML colon-space hazard, Agent tool model granularity, skill content lifecycle, auto-compaction 25K re-attachment budget, skill listing 1,536-char cap, forked-skill context inheritance) and cross-links scattered facts (subagent isolation, MCP constraints, dispatch convention) without consolidating. (b) `.claude/sync-manifest.json` — registered in `sync` array. (c) `.claude/CLAUDE.md § Navigation` — new row pointing to authoring doc. (d) `.claude/agents/implement-agent.md § "Editing strategy"` — YAML colon-space bullet (em-dash convention) with cross-ref. (e) `.claude/rules/agents.md` — capability-grounding cross-refs in § State Ownership + § Command Invocation Gates. (f) `.claude/commands/iterate.md § "Step 4: Propose Changes"` — capability-claim cross-check before proposing spec text. (g) `.claude/commands/work.md § "If Decomposing"` — capability-claim cross-check during decomposition. (h) NEW `.claude/commands/health-check.md § "Part 2d: Capability Doc Freshness"` — reads footer; 90-day warning threshold + 180-day strong warning; `[V] Verify` user-adjudicated WebFetch action diffs against current doc body + user accepts/rejects per section; `[D] Defer` writes sentinel to `dashboard-state.json` for 30 days. (i) `.claude/version.json` — 4.8.0 → **4.9.0** (MINOR). (j) `.claude/dashboard.md` META — `template_version` bump. **Why Option B over alternatives:** Option A (footer-only) ships visible accountability without active mechanism — worst-of-both. Option C (WebFetch auto-sync) carries silent-contradiction risk between synced doc and current spec — auto-sync's immediacy value is also its failure mode. Option D (version-pinning) decouples doc cadence from Claude Code's release cadence — high maintainer burden. Option B preserves user adjudication authority while delivering active drift signal at the existing `/health-check` cadence. **Deferred:** `/audit-coherence` capability-claim lens (retroactive sweep over spec text) — Wave 2 if signal accumulates. Active feedback.md FB-082 + FB-083 replaced with PROMOTED stubs pointing to archive entries.

- **v4.8.0 (2026-05-24)** — 5-FB cheap-action bundle from `/feedback review` manual walk-through on `template-maintenance/feedback.md` (FB-006 / FB-074 / v4.7.3 multi-cheap-action-ship precedent). Per-research dispositions in `.claude/support/workspace/fb-{078,081,086,088,089}-research.md`. **FB-078** — `phase-decision-gates.md § "Post-Decision Check"` extended with Step 2a.1 chosen-option no-op scan (4 canonical markers + contradicting-phrase guard). Catches the false-positive case where `inflection_point: true` decisions land on close/defer/no-op options. **FB-081** — heartbeat (`commands/work.md § Step 3 "Autonomous batch heartbeat"`) + ping-mid-batch behavioral rule (`rules/agents.md § "Behavioral Rules" — "Acknowledge mid-batch user messages"`) shipped together, sharing `autonomous_batch_position >= 3` threshold. Heartbeat is Tier 2 inline only (NOT dashboard write — violates Recent Activity 7-entry cap). **FB-086** — verify-agent.md Step T2b extended with step 4b distinguishing drift (same-directory undeclared + `[Multi-file]` flag) from violation; emits `verification_gap` friction marker; `commands/work.md § "After verify-agent returns" step 8` auto-updates task JSON's `files_affected` to match actual; `friction-register.md` extended `verification_gap` row with sub-use note. Pre-pass parallel surface (FB-058 6th heuristic) deferred — verify-time check has ground truth. **FB-088** — new `commands/work.md § "Step 0e: Uncommitted-Work Check"` between Step 0d and Step 1; always runs but surfaces only when N≥3 finished tasks since last commit AND non-zero modified/untracked source files; heuristic-only `.claude/` exclusion filter; dashboard sentinel deferred. **FB-089** — new `commands/work.md § "Step 0f: Track 2 Stale-File Recovery"` after Step 0e; Option 1.5 (recover-by-compile-then-cleanup) shipped; new `export_quality: "recovered"` enum value; orphaned `.interaction-assessment.json` + `.session-log.jsonl` compiled into `.session-export-{timestamp}-recovered.json` then deleted; no PreCompact hook coordination needed (disjoint Track 2 territory). **Files touched (7):** `.claude/support/reference/phase-decision-gates.md`, `.claude/commands/work.md` (4 sections), `.claude/rules/agents.md`, `.claude/agents/verify-agent.md`, `.claude/support/reference/friction-register.md`, `.claude/version.json`, `.claude/dashboard.md` (META `template_version` bumped 4.7.3 → 4.8.0). MINOR per SemVer (new behavior surfaces — heartbeat, ping-rule, Step 0e, Step 0f, T2b drift detection; no breaking change). No DEC (per-research dispositions all converged on cheap-action-no-DEC; Option 1.5 for FB-089 dissolves the "two viable approaches" framing in the original capture). No sync-manifest change. Active feedback.md entries (FB-078/081/086/088/089) replaced with PROMOTED stubs pointing to archive + workspace research docs. Also: **FB-082 + FB-083 bundled** for DEC opening (research complete; matrix populated; user elected to open DEC for the freshness-mechanism choice). All 6 research-gated items from the 2026-05-24 walk-through dispatched in parallel; 5 of 6 shipped here, 1 (FB-082+083) proceeds to DEC.

- **v4.7.3 (2026-05-24)** — `/health-check` triage pass on `template-maintenance/feedback.md` shipped FB-084 + FB-087 as bundled cheap actions (FB-006 / FB-074 multi-cheap-action-ship precedent). **FB-084** — new `## Pre-Retirement Engine-Consumer Audit` section in `.claude/rules/feature-retirement.md` between "When to Use This Workflow" and "Procedure" (4-pattern grep checklist: snake_case + CamelCase derivatives + shortened forms + string literals; ~12 lines). Source: styler 2026-05-22 session (FR-014 / T714 — snake_case-only grep concluded "no engine consumer" for `price_quality_philosophy`; actual engine consumed via `PriceQualityPhilosophy` type + `RankerSignals.philosophy` shortened field + `PHILOSOPHY_WEIGHTS` constant; retirement landed and would have silently degraded runtime). **FB-087** — new `## MCP and Result-Size Constraints` section in `.claude/rules/agents.md` between "## MCP and Parallel Execution" and "## Tool Preferences" (~8 lines). Documents `browser_snapshot` token-budget truncation on long-scroll pages (~10K+ char DOM) and the `browser_evaluate`-with-targeted-queries workaround. Source: styler 2026-05-21 session (105K+ char snapshots on `/style` page). Also: FB-085 marked `deferred + signal-gated (2nd-project)` — proposed `agents.md` behavioral rule has fragile enforcement scope (verify-agent subagent sandbox limits Playwright access). FB-086 marked `research-light-candidate + ready` — git diff cross-check is mechanical but friction-kind schema + timing scope need `/research`. All four FB entries from the 2026-05-24 `/health-check` Part 7 aggregation now triaged. **Captures from this triage pass:** 0 (the pass resolved the four prior captures; no new signals surfaced). **Files touched:** `.claude/rules/feature-retirement.md`, `.claude/rules/agents.md`, `.claude/version.json`, `.claude/dashboard.md` (META `template_version` bumped 4.7.2 → 4.7.3 to match version.json), `template-maintenance/feedback.md` (FB-084-087 status/disposition lines), root `CLAUDE.md` (this entry + Active Follow-ups additions for FB-084 / FB-085 / FB-086). No DEC; no sync-manifest change (both rule edits are content-only on existing sync-category files).

- **v4.11.1 (2026-05-27)** — **Command-table doc reconciliation** (PATCH, no FB — surfaced while addressing a Claude Code "Large CLAUDE.md" performance warning on the root template-maintenance file, same session as the ship-log extraction noted in this file's header). Two user-facing command references had drifted from the actual `.claude/commands/*.md` set (14 commands): `.claude/CLAUDE.md § "Environment Commands"` was missing `/diagnose` + `/zoom-out` + `/audit-coherence` + `/audit-ui`; `.claude/README.md § "Commands"` was additionally missing `/grill` + `/shakedown`. Both tables now enumerate all 14 commands. Root cause: the two help-me-think commands (`/diagnose` v4.4.0, `/zoom-out` v4.3.0) shipped without a CLAUDE.md/README table row, and the audit family (`/audit-coherence` + `/audit-ui`, v3.6.0–v3.12.0) plus the Wave-1/2 commands (`/grill`, `/shakedown`) were never added to the README table. `/audit-coherence` + `/audit-ui` listed with a "usually dispatched by `/health-check`" note (both have direct `triage` modes). **Files touched:** `.claude/CLAUDE.md` (sync — 4 rows added), `.claude/README.md` (customize — 6 rows added), `.claude/version.json` (4.11.0 → 4.11.1), `.claude/dashboard.md` (META `template_version` bump). PATCH per SemVer (documentation completeness; no new commands, no behavior change — consistent with v4.7.2 dashboard-refresh precedent). No DEC; no sync-manifest change.

- **v4.12.0 (2026-05-27)** — **DEC-020: concluded the DEC-007 Skills/reference duplication trial via Option A (retire the 3 Skills, keep the reference docs).** The ~40-day open-ended trial (DEC-007 Option B, 2026-04-17) had no exit criterion and had drifted — the decomposition SKILL led its reference by 52 lines because the FB-073/v4.5.0 `Test-Protocol Runtime Constraints` section was added to the SKILL only. Surfaced from the v4.11.0 "Large CLAUDE.md" session; routed through `/research` → `decisions/decision-020-conclude-skills-reference-trial.md` (+ archive `decisions/.archive/decision-020-research-2026-05-27.md`). **Research basis:** Skill auto-fire is documented-as-unreliable and unobservable in-session (the only precise signal is opt-in OTel `claude_code.skill_activated` / `invocation_trigger: claude-proactive`, captured nowhere in this repo), whereas the orchestrator path-cites the reference docs (observed value) — so on principle the proven-value location is the refs; the evidence-first posture (DEC-018) argues against retiring the proven location for the unproven one. **What shipped:** (a) migrated the `Test-Protocol Runtime Constraints` section into `.claude/support/reference/decomposition.md` (the only real drift; spec-checklist + dashboard drift was whitespace-only); (b) deleted `.claude/skills/` (3 SKILLs), guarded on the migration being present; (c) removed the 3 `<!-- mirror -->` comments; (d) re-pointed the 2 live SKILL-path citations — `support/reference/task-schema.md` + `rules/dashboard.md` — to the reference docs; (e) cleaned ownership lists (`README.md` Skills section + Essential-Files + Where-to-Find rows; `rules/agents.md`; `support/reference/extension-hooks.md`) + `sync-manifest.json` (dropped `.claude/skills/*/SKILL.md`) + the `health-check.md` illustrative sync snippet; (f) `DEC-020` → `implemented`, `DEC-007` annotated with the conclusion. **Files touched (11):** the 3 reference docs, `sync-manifest.json`, `README.md`, `rules/agents.md`, `rules/dashboard.md`, `support/reference/{task-schema,extension-hooks}.md`, `commands/health-check.md`, `version.json`, `dashboard.md` (META) + the 2 decision records + this log. MINOR per SemVer (feature retirement / significant behavior change; non-breaking — nothing referenced the Skills functionally). Also captured **FB-094** (`claude-code-authoring.md` skill-listing-budget drift, surfaced during the research) in `template-maintenance/feedback.md`.

- **v4.12.1 (2026-05-27)** — **FB-094 promoted + FB-091/FB-092 declined** during a `/feedback review` walk-through of `template-maintenance/feedback.md` (FB-090/v4.10.1 same-session-capture→ship precedent). **FB-094** (capability-doc drift, surfaced during DEC-020): rewrote `.claude/support/reference/claude-code-authoring.md § "Skill listing budget"` to separate the **dynamic total** listing budget (~1% of the model's context window; raisable via `skillListingBudgetFraction` setting / `SLASH_COMMAND_TOOL_CHAR_BUDGET` env var; least-invoked descriptions dropped first on overflow, names always kept) from the **per-entry 1,536-char cap** (`description`+`when_to_use`; configurable via `maxSkillDescriptionChars`) — the old heading/body conflated the two. Added a `/doctor` (overflow + which-skills-affected) + `/skills` (visibility menu) observability line. **Verified-then-fixed** per the user-chosen rigorous path: points 1+2 confirmed against `code.claude.com/docs/en/skills` (2026-05-27); point 3's `/context`-as-load-inspector claim **dropped** as unverified (the page documents `/skills` as a visibility menu, not a presence inspector, and doesn't mention `/context`). Footer `Last verified` → 2026-05-27 + `template_version` → 4.12.1. This is the DEC-017 (v4.9.0) capability-doc freshness case the `/health-check` Part 2d `[V]` lens was built to catch, actioned early. **FB-091 + FB-092** (sibling single-source orchestrator-bash-hygiene signals from the 2026-05-25 insights report) **declined**: FB-091 (guard speculative bash probes) — common form already covered by the Tool Preferences `Edit`-not-bash-chain rule; residual too thin + unreproduced. FB-092 (absolute paths / "don't assume CWD persists") — core premise contradicted by the Bash harness contract (CWD *does* persist; only shell state doesn't), absolute-path half already implied; only the `>`-vs-`>>` session-log residual was concrete, too thin for a rule. Both archived `closed` (re-open on a real session-export reproduction). **Files touched:** `.claude/support/reference/claude-code-authoring.md` (section + footer), `.claude/version.json` (4.12.0 → 4.12.1), `.claude/dashboard.md` (META bump), `template-maintenance/feedback.md` (3 stubs: FB-091/092 closed, FB-094 promoted), `template-maintenance/feedback-archive.md` (3 full entries). PATCH per SemVer (mechanical doc correction + feedback triage; no behavior change, no new surface). No DEC; no sync-manifest change (`claude-code-authoring.md` already in `sync`).

- **v4.13.0 (2026-06-10)** — **FB-085 promoted: `/diagnose` visual recipe shipped** (2nd-project gate overridden by user decision 2026-06-10). New `## Visual / browser-rendering bugs` section in `.claude/commands/diagnose.md` (between Phase 6 and Out of scope; ~20 lines) implementing the locked design from the 2026-05-24 `/visual-verify` grill verbatim: orchestrator-level browser loop (subagent MCP-inheritance + single-session constraints honored; ToolSearch load note; dev-server + respect-prior-kills note); contract = falsifiable assertions on measured values (geometry + computed style incl. sampled interaction states), pixel-diffs/golden images excluded; `browser_evaluate` targeted-query measurement with `browser_take_screenshot` demoted to reporting-only (FB-087); N=3 configurable loop, one falsifiable hypothesis per iteration with a pre-computed predicted value-effect (absorbs the 2026-05-20 signal-queue "math-check before commit-to-pixels" item), early-exit when out of distinct hypotheses; non-convergence surfaces unmet contract + hypothesis→prediction→result trace + before/after screenshots, never silent; persistence conditional on a correct-seam test harness (Phase 5 logic). **Gate-override rationale** (recorded in FB-085 archive entry + root `ship-plan-1-evidence-based-verification.md § S1`): the 2nd-project gate predated the grill that fixed cost (~20 lines, zero new surface, trivially reversible); within-styler evidence volume grew past the gate's intent (62/391 session-export friction notes verification-themed; T731 verified-pass-but-500; UI-drift dominant theme in styler's archived feedback; 806 verifications with zero terminal fails). General-merit half (Phase 3 outcome-not-mechanism) had shipped v4.10.2. **Also this session: FB-011 Family C trigger declared MET** on substituted evidence (4b channel structurally unsampled — `/health-check` ~7 mentions across the 47-session corpus; substituted standard = recurrent post-fix dashboard regressions, FB-015→038→080→090 + v4.7.2; amendment recorded in `template-maintenance/scripts-candidates.md § Family C`); scope = PoC only (Tasks-by-phase render, byte-identical re-render test), full port gates on PoC. **Files touched:** `.claude/commands/diagnose.md` (recipe), `.claude/version.json` (4.12.1 → 4.13.0), `.claude/dashboard.md` (META bump), new `tests/scenarios/32-diagnose-visual-recipe.md` (non-convergence + arbiter-discipline + orchestrator-level traces), `template-maintenance/feedback.md` (FB-085 → PROMOTED stub; math-check signal-queue item → absorbed), `template-maintenance/feedback-archive.md` (FB-085 full entry), `template-maintenance/scripts-candidates.md` (4 Family C edits), root `CLAUDE.md` (Active Follow-ups: FB-085 bullet → promoted note; FB-011 line updated). MINOR per SemVer (new behavior surface in an existing command; no breaking change). No DEC (the fold fails the hard-to-reverse criterion, per FB-085's recorded reasoning). No sync-manifest change (`.claude/commands/*.md` glob auto-covers).

- **v4.13.1 (2026-06-10)** — **Pointer-rot + consistency sweep** (PATCH, no FB — Plan 2 P1 from the 2026-06-10 cross-repo analysis; staged in root `ship-plan-2-prose-diet-and-mechanization.md § P1`, temporary working file). Four fix groups: **(1) Stale line-number cross-refs** — `rules/agents.md § Dispatch Convention` cited work.md "line ~605/~688" + research.md "line ~74" (~150 lines stale); replaced with section-name anchors (§ "If Verifying (Per-Task)", § "If Verifying (Phase-Level)", § "Step 3: Spawn Research Agent") + codified "section names, not line numbers" for cross-file refs. Repo-wide grep confirmed this was the only line-ref instance. **(2) Phantom citation** — `commands/audit-coherence.md` path-drift lens used `.claude/support/reference/audit-template-pattern.md` (a file that has never existed in the repo) as its example of "template file legitimately absent in a project"; replaced with the real `audit-fix-workflow.md`. **(3) Model-pin single-sourcing** — `.claude/CLAUDE.md § Model Requirement` is now the declared canonical source for BOTH the design pin (Opus 4.7) and the `Task` dispatch value (`model: "opus[1m]"`), with the alias-vs-pin relationship documented honestly (the alias tracks the latest Opus; the pin records the tested version; divergence handling = deliberate explicit-ID pin, not silent drift). Nine restatement sites now cite it: work.md (prose + 2 Task blocks via citation comments), research.md Task block, the 3 agent-file headers (hardcoded "Opus 4.7 / always set opus[1m]" dual statement removed entirely), `rules/agents.md § Model Requirement`, `parallel-execution.md § 3` (which falsely claimed the alias "ensures" Opus 4.7), and `shared-definitions.md` difficulty-calibration note (pin pointer added). **No dispatched value changed** — this documents de-facto behavior (the alias governs at dispatch; the pin records design target) and kills the silent contradiction. **(4) `/status` icon vocabulary** — `commands/status.md § Status Icons` annotated as display-layer labels (✅ explicitly mapped to canonical task status "Finished"; ⚠️/🔴/🚀 identified as timeline-health annotations computed from dates, not statuses; canonical 8 unaffected). **Flagged, not edited (out of PATCH scope):** `claude-code-authoring.md` § Tool & Dispatch Surface states the Agent tool's `model` param exposes only `sonnet | opus | haiku` — the live harness (observed this session) also exposes `fable`; route through `/health-check` Part 2d `[V]` verification per DEC-017 discipline before editing the capability doc. Also left as-is: `automation.md`'s CLI example `--model claude-opus-4-7` (copy-paste example; explicit ID is appropriate there). **Files touched (13):** `rules/agents.md` (2 edits), `commands/audit-coherence.md`, `.claude/CLAUDE.md`, `commands/work.md` (3 sites), `commands/research.md`, `agents/implement-agent.md`, `agents/verify-agent.md`, `agents/research-agent.md`, `commands/status.md` (2 edits), `support/reference/parallel-execution.md`, `support/reference/shared-definitions.md`, `version.json` (4.13.0 → 4.13.1), `dashboard.md` (META bump). PATCH per SemVer (consistency/doc corrections; zero behavior change). No DEC; no sync-manifest change (all files already in `sync`).

- **v4.14.0 (2026-06-10)** — **"Waiting-on-you" queue contract** (Plan 3 T3 from the 2026-06-10 cross-repo analysis; root `ship-plan-3-interaction-tax-and-queue.md § T3`). New **human-gated coverage invariant**: every item blocked on the user — `owner: human` tasks with satisfied deps, `owner: both` awaiting review, On Hold tasks, unresolved decision records, unanswered questions from a paused session — MUST have a dashboard 🚨 Action Required row with the concrete question/action inline; handoff prose must never be a blocking item's only home. **Evidence basis:** styler handoff held two decisions "PAUSED mid-decision (unanswered)" + 5 On Hold tasks gated on the user with no consolidated surface; 25 handoff-themed friction notes across 76 session exports. **What shipped:** (a) new `commands/work.md § Step 0g: Waiting-on-You Queue (always runs)` — session-start enumeration (task scan + unresolved decisions + handoff unanswered questions) printed before routing, with a dashboard cross-check that adds missing rows via the FB-080 targeted-edit path; (b) new open-question sweep bullet in `commands/work.md § Context Transition (/work pause)` key rules — sweep runs BEFORE the handoff write, each item lands as an Action Required row, handoff points at rows; (c) invariant documented at both dashboard spec sites: `rules/dashboard.md § Sections` (Action Required bullet) + `dashboard-regeneration.md § Action Item Contract` (new "Coverage (human-gated items)" clause — completeness is now the contract's second half alongside per-item quality); (d) new trace test `tests/scenarios/33-waiting-on-you-queue.md` (pause sweep / start print / empty-queue silence). **Files touched:** `commands/work.md` (2 sites), `rules/dashboard.md`, `support/reference/dashboard-regeneration.md`, new `tests/scenarios/33-waiting-on-you-queue.md`, `version.json` (4.13.1 → 4.14.0), `dashboard.md` (META bump). MINOR per SemVer (new behavior surface: Step 0g + pause sweep). No DEC; no sync-manifest change.

- **v4.15.0 (2026-06-10)** — **Evidence-carrying verification bundle (Plan 1 S2+S3+S4)** from the 2026-06-10 cross-repo analysis (root `ship-plan-1-evidence-based-verification.md`). Evidence basis: styler's 806 verifications with zero terminal fails while the user caught shipped UI drift post-ship; T731 verified-pass-but-500; FR-040 false "dormant" finding from a silently-failing bash grep. **S2 — Negative findings require a positive control:** new `rules/agents.md § "Negative Findings Require a Positive Control"` — absence claims ("unused/dormant/no consumer/never fires") persist to durable state only when produced by the `Grep` tool OR accompanied by a positive control (same probe finding a known-present target); otherwise "unverified absence", no state write. Verify-agent Tool Preferences paragraph wires report `issues[]`/`friction_markers[]` to the rule. Complements FB-084 (*which patterns* to search) with *whether the probe works at all*. **Deviation from plan:** the `claude-code-authoring.md` BSD-grep note was NOT added — it's an OS-tooling fact, not a Claude Code capability fact; the why lives inside the rule itself (avoids the doc-duplication class Plan 2 targets). **S3 — Empirical Evidence Gate:** new optional `task_verification.evidence[]` (schema + worked example at `task-schema.md § "Evidence Sub-field"`; types `http_status`/`console`/`computed_style`/`geometry`/`screenshot`/`build`); verify-agent Step T4b item 5 names `empirical_assertions[]` for the orchestrator to run (subagents lack reliable browser-MCP access); `commands/work.md § "If Verifying (Per-Task)"` gains the gate — orchestrator-level, before persisting a web-UI pass (ToolSearch Playwright load; `browser_evaluate` targeted queries per FB-087; **FB-076 mitigation 1**: production-build check when client-marked files touched + root `./CLAUDE.md § Verification Hooks` declares a build command — new section in `root-claude-md-template.md`); any failing assertion routes through the normal fail path; State Persistence step 3 writes `evidence[]`. Plan open-question resolved per its lean: NO `runtime_validation` enum split (`partial_empirical`/`partial_documented` rejected) — empirical-ness is derived from `evidence[]` presence, zero migration. **S4 — Phase UI smoke:** `commands/work.md § "If Verifying (Phase-Level)"` — before acting on a phase `pass`, phases that touched web-UI routes get a lite orchestrator pass (HTTP status + console scan + accumulated-evidence re-check); failures create fix tasks and loop to Execute; `/audit-ui` referenced as the deeper escalation (puts audit-ui-class checking inside the loop that actually runs — usage data showed zero voluntary `/audit-ui` invocations ever). All three gates skip non-web tasks/projects entirely — the domain-agnostic contract is preserved. **Trace test:** new `tests/scenarios/34-evidence-verification.md` (uncontrolled-absence non-persistence / evidence gate + build check + fail-flip / phase smoke). **Ledger:** FB-076 → mitigation 1 shipped (maintenance entry + root `CLAUDE.md` line updated; mitigations 2–3 remain deferred, research-gated). **Files touched (12):** `rules/agents.md`, `agents/verify-agent.md` (2 sites), `support/reference/task-schema.md` (2 sites), `commands/work.md` (3 sites), `support/reference/root-claude-md-template.md`, new `tests/scenarios/34-evidence-verification.md`, `version.json` (4.14.0 → 4.15.0), `dashboard.md` (META bump), plus maintenance-side `template-maintenance/feedback.md` (FB-076 status) + root `CLAUDE.md` (FB-076 line). MINOR per SemVer (new behavior surfaces: rule, gate, smoke, optional schema field; no breaking change — `evidence[]` optional, gates conditional). No DEC; no sync-manifest change (`root-claude-md-template.md` already in `sync`; tests/ doesn't ship).

- **v4.16.0 (2026-06-10)** — **Lazy-load context diet (Plan 2 P2)** from the 2026-06-10 cross-repo analysis (root `ship-plan-2-prose-diet-and-mechanization.md § P2`). **Measured result: per-session auto-loaded payload 67,992 → 45,673 bytes (−22.3K, −33%).** Three moves, each leaving an explicit READ-trigger at the original site: **(1) `rules/feature-retirement.md` un-imported** (15.9K; needed only mid-retirement) — removed from `.claude/CLAUDE.md` `@imports`; its Workflow Rules summary line became the trigger ("**lazy, NOT auto-loaded:** before retiring or restoring ANY feature, READ ... first"). File unchanged on disk; sync category unchanged. **(2) `rules/agents.md` −4.1K:** "MCP and Parallel Execution" + "MCP and Result-Size Constraints" moved verbatim to new `support/reference/mcp-patterns.md` (registered in sync-manifest); "Cross-Project Capture Protocol" moved verbatim into `support/reference/extension-hooks.md` (its natural home — the protocol's canonical-map cross-ref is now in-document; 3 self-references in extension-hooks fixed). All three sections keep H2 stubs in agents.md under the original names carrying the single most load-bearing one-liner + the READ trigger, so existing `§`-citations resolve and the critical warning survives even without the read. Six citer paths updated to the new home: `diagnose.md` ×2, `audit-ui.md`, `health-check.md` Part 8, `claude-code-authoring.md § MCP Constraints` ×2 (pointer-path swap only — no capability claims changed). **(3) `rules/session-management.md` −2.6K:** user-facing content (Resuming table + `/rename`, Managing Context Pressure, When to Use Plan Mode, Checkpointing and Rewind) moved to new `.claude/README.md § "Session Operations"`; the rules file keeps what Claude must act on (Ending Sessions, Which-Mechanism table, Plans core, What-Survives table, Claude's Responsibilities) + a pointer. **Deviation from plan:** dispatch-convention rationale paragraphs stay in agents.md (~1.5K — small, and moving them to the capability doc would expand this ship's verify-then-fix surface; revisit with P3 if needed). **Trace test:** new `tests/scenarios/35-lazy-rule-triggers.md` (retirement-trigger read / MCP-batch read / capture-protocol read-before-sync; includes the negative checks — lazy docs NOT read when their flow isn't active). **Files touched (15):** `.claude/CLAUDE.md` (imports + 3 summary lines), `rules/agents.md`, `rules/session-management.md`, new `support/reference/mcp-patterns.md`, `support/reference/extension-hooks.md`, `.claude/README.md`, `commands/diagnose.md`, `commands/audit-ui.md`, `commands/health-check.md`, `support/reference/claude-code-authoring.md`, `sync-manifest.json`, new `tests/scenarios/35`, `version.json` (4.15.0 → 4.16.0), `dashboard.md` (META bump). MINOR per SemVer (loading-behavior change + new reference file; no content removed from the template, only relocated). No DEC; sync-manifest change: +1 file.

- **v4.17.0 (2026-06-10)** — **Audit-family shared core (Plan 2 P4)** from the 2026-06-10 cross-repo analysis (root `ship-plan-2-prose-diet-and-mechanization.md § P4`). New `support/reference/audit-family-core.md` (17.1K, sync-registered) — the single source for the audit family's duplicated machinery; `commands/audit-coherence.md` 46.9K → 37.0K and `commands/audit-ui.md` 45.8K → 39.8K (−15.8K duplicated surface). **What moved:** (1) **Promote mode** — canonical 10-step algorithm + FB-entry template + dedupe options (`[S]/[U]/[M]/[N]`) + cascades; both commands now delegate with substitution rows (`{AUDIT}/{P}/{DIR-GLOB}/{CMD}`); previously audit-coherence said "same shape as /audit-ui promote" then restated all 10 steps anyway. (2) **Fix mode** — canonical 7 steps + run-tests-after-removal rule; commands keep one-paragraph delegations (audit-ui keeps its bundle-eligible-is-rare note). (3) **Triage mode** — the full canonical algorithm (resolution, empty states, per-finding loop, per-kind action gates, atomic state mutations, edge cases) moved from audit-coherence (where ~120 lines lived) to core; BOTH commands now delegate (audit-ui previously delegated to audit-coherence — now both point at the neutral home). (4) **Synthesizer shared contract** — description constraints + kind classification, **unified**: the two copies had already drifted (coherence had the DEC-013 Q3 `transitive_consumer_risk` clause on orphan removals; ui lacked it) — the unified rules apply it to both audits. Delivery mechanism: **splice-at-dispatch** — the orchestrator inserts the core sections' verbatim text at the prompt's `{SPLICE HERE}` marker before dispatching, so synthesizer sub-agents receive inline text, never a read-this-file instruction (execution reliability; same philosophy as lens prompts passed verbatim). (5) **digest.json schema — shipped canonical:** both commands previously cited "Component 2 schema" in `template-maintenance/audit-command-family-proposal.md`, **a file that doesn't ship to projects** (latent phantom for downstream); the full item/annotation schema (from the proposal § "2. Audit command interface", incl. the v3.18.0 description-vs-title note and the status enum) now lives in core as the shipped canonical, with the proposal cited as design history. **Also fixed:** audit-coherence usage block's promote example said `F-01,F-07` (audit-ui's prefix) — corrected to `C-01,C-07`. **Deviation from plan:** lens-output-format extraction CUT — on inspection the two formats differ materially (coherence: Source anchor + Files affected; ui: Page + Element); the duplication is structural, not verbatim, and lens prompts must stay inline-complete for sub-agent reliability. **Trace test:** new `tests/scenarios/36-audit-family-core-delegation.md` (substitution correctness / splice-inline + unified-classification / cross-command promote consistency). **Files touched (8):** new `support/reference/audit-family-core.md`, `commands/audit-coherence.md` (6 sections), `commands/audit-ui.md` (6 sections), `sync-manifest.json` (+1), new `tests/scenarios/36`, `version.json` (4.16.0 → 4.17.0), `dashboard.md` (META bump), `ship-plan-2` (P4 ✅). MINOR per SemVer (structural relocation + drift-unification; one behavioral fix — ui synthesizer now applies `transitive_consumer_risk`). No DEC; sync-manifest change: +1 file.

- **v4.18.0 (2026-06-10)** — **work.md decomposition (Plan 2 P3)** from the 2026-06-10 cross-repo analysis (root `ship-plan-2-prose-diet-and-mechanization.md § P3`). `commands/work.md` 73.6K/1,184 lines → **59.5K/926 lines**; the four extracted procedures are exactly the prose-skipped-under-load class the ship history traces ~20 work.md patches to. **What moved (content fidelity diff-verified against git HEAD — every delta is a documented reference adaptation, zero content loss):** (1) **State Persistence Protocol** (all three after-return protocols incl. the DEC-011 dual-write friction append, audit-register projection, FB-086 drift update) → new `support/reference/work-procedures.md` (17.3K, sync-registered); (2) **Task Completion (`/work complete`)** (10-step process: verification enforcement + self-attestation, deliverable validation, two-prompt notes collection, cascades) → same file; (3) **Auto-Archive** → same file; (4) **Interaction Assessment + Session Export** (Track 2 pause artifacts) → `support/reference/context-transitions.md` new § "Pause Follow-Through (Track 2 — Cross-Project Logging)" — the file the pause procedure already directs the orchestrator to read. **Stub design:** each extracted section keeps its original heading in work.md (all internal `§`-references still resolve — 6 "State Persistence Protocol" mentions intact) + a **STOP — read X NOW** gate + a hard-invariant summary explicitly marked non-executable ("do not improvise from this summary; the procedure file is the contract"). Delegation precedent: Step 1a/1b → drift-reconciliation.md. **Process note:** the first extraction attempt mis-stitched the protocol body from two non-contiguous session reads (dropped the `blocked`/`misaligned` bullets + the DEC-011 dual-write step); caught by Edit's exact-match failure, then corrected and **verified by diffing each removed block (git HEAD line ranges) against its new home** — the verification step is the lesson, recorded here for future extractions. **Honest target note:** the plan's ≤40K aspiration was NOT reached (59.5K); the remaining mass is step skeleton, routing logic, dispatch prompt templates, and Step 0 recovery machinery the plan said to keep inline. Revisit only if work.md misfires persist post-split. **Trace test:** new `tests/scenarios/37-work-procedure-stubs.md` (agent-return gate read + dual-write invariant / `/work complete` verification enforcement / pause Track 2 follow-through). **Files touched (8):** `commands/work.md` (4 sections → stubs), new `support/reference/work-procedures.md`, `support/reference/context-transitions.md` (+§), `sync-manifest.json` (+1), new `tests/scenarios/37`, `version.json` (4.17.0 → 4.18.0), `dashboard.md` (META bump), `ship-plan-2` (P3 ✅). MINOR per SemVer (structural relocation; loading behavior of /work sub-procedures changes to gate-read). No DEC.

- **v4.19.0 (2026-06-10)** — **Family C PoC: `dashboard-render.py` (Plan 2 P5** — final Plan 2 item; root `ship-plan-2-prose-diet-and-mechanization.md` now fully shipped). New `.claude/scripts/dashboard-render.py --tasks-section` — deterministic renderer for the dashboard's `## 📋 Tasks` section, implementing the Section Display Rules from `dashboard-regeneration.md`: phase grouping with `Phase {N} — {name}` headers + numeric-aware sort (subtask ids like `2_1` order correctly), completed-phase collapsing (`✅ N tasks finished`), >10-finished summarization in active phases, blocked-phase collapsing (>5 non-actionable, deterministic blocker summary from decision deps + unfinished task deps, max 2 + "and N others"), per-task status displays (⏸️ On Hold, `Absorbed → Task {id}` excluded from counts, `Finished (N retries)` from verification_history, conflict-note held-state, ⚠️ out-of-spec prefix, `(cross-phase)` suffix), merged Deps column (task + decision deps), per-phase and overall progress footers matching the golden example. Stdlib-only, read-only, exit codes per the scripts invocation contract; `archive/` excluded. **Tests:** 13 new in `scripts/tests/test_dashboard_render.py` — headlined by the **byte-identical re-render gate** (the PoC acceptance criterion from scripts-candidates § Family C) plus file-order independence and per-rule pins; full suite 25/25 green. **Known PoC simplification** (documented in the script docstring + scripts-candidates): per-phase qualifier lines are deterministic count enumerations rather than the example's judgment-flavored phrasing. **NOT wired into the regen flow** — advisory per `scripts/README.md` (new table row + status paragraph); the **full-port decision** (script renders all structural sections between marker pairs; LLM keeps Action Required + Notes; `dashboard-regeneration.md` § structural rules then largely retire into the script) gates on diffing PoC output against a real project's LLM-rendered Tasks section — suggested: run in styler. **Also captured: Family F candidate** in `scripts-candidates.md` — post-batch state-invariant checker (validates the State Persistence Protocol *ran*: markers appended, transitions legal, expected files exist; the mechanized backstop if prose-skipping survives the v4.18.0 split; trigger = post-v4.18.0 telemetry still showing skipped steps). **Ledger:** FB-011 line in root `CLAUDE.md` updated (Family C → PoC shipped). **Files touched (8):** new `scripts/dashboard-render.py`, new `scripts/tests/test_dashboard_render.py`, `scripts/README.md`, `template-maintenance/scripts-candidates.md` (Family C status + Family F), root `CLAUDE.md` (FB-011), `version.json` (4.18.0 → 4.19.0), `dashboard.md` (META bump), `ship-plan-2` (P5 ✅). MINOR per SemVer (new shipped script + tests; advisory, no behavior change to the regen flow). No DEC (PoC is the evidence-gathering step for the future full-port decision). No sync-manifest change (`.claude/scripts/*.py` + `tests/*.py` globs auto-cover).

- **v4.20.0 (2026-06-11)** — **`/iterate` single-response batch approval (Plan 3 T1)** from the 2026-06-10 cross-repo analysis (root `ship-plan-3-interaction-tax-and-queue.md § T1`). **Evidence basis:** iterate.md carried 20 bracket menus + 32 approval phrases; the old Step 4→5 gate required every `[NEEDS APPROVAL]` item resolved individually "even to `[Y] Apply all`" — a user choosing Apply-all still answered N prompts; session corpus shows short, frequent, pause-disciplined sessions where per-session ceremony never amortizes. Counter-evidence honored: all 5 explicit Erik corrections in the corpus re-anchor framing/scope — batch UI compresses *responses*, never *information*. **What shipped (all in `commands/iterate.md`):** (1) Step 4 "Decisions in This Proposal" format now numbers `[NEEDS APPROVAL]` items (`N1`, `N2`, …) so a batch response can address them individually; enumeration + origin tags + proposed text unchanged (FB-032 structural contract fully intact). (2) Step 4 approval prompt replaced: `[Y]` Apply all (every item approved as listed) · `N{n}: <choice>` override specific items, rest as listed · `[M]` walk item-by-item (pre-v4.20 guided pass, preserved) · `[P]` postpone (nothing applied; old `[N] Skip` folds in, old `[P] Partial` subsumed by overrides, old `[M] Modify` survives as free-text → revise-and-re-present). (3) Step 5 mandatory gate reworded to batch-resolution semantics: bare `[Y]` checks all open items approved-as-listed with NO follow-up per-item prompts; overrides resolve named items + rest as listed (revised text shown at apply; full re-present only when an override materially alters other changes); unresolved leftovers (e.g., override names nonexistent `N{n}`) block apply and ask about those only; explicit "Visibility is non-negotiable (FB-032)" clause forbids trimming the table; DEC-016 clause records that compliance is unchanged (declaration = audit trail of intent; `permissions.ask` still fires on the spec write; DEC-016 requires routing through `/iterate`, never N serial prompts). (4) Distill step 6 cross-ref updated to the new letter semantics (`[P]` postpones without creating anything). **FB-033 watch (explicit, per plan):** the spec-auditor trial gate keys on silent-decision friction — this ship is a UX change (response compression), NOT a contract change; items remain fully enumerated with proposed text before the single response. FB-033's trial should read post-v4.20 `/iterate propose` sessions with that framing when assessing whether silent-decisions friction persists. **Trace test:** new `tests/scenarios/38-iterate-batch-approval.md` (bare-Y zero-follow-up / override resolution + nonexistent-N{n} block / [M] walk + [P] postpone / FB-032 information-content invariance). **Files touched (5):** `commands/iterate.md` (4 sites), new `tests/scenarios/38-iterate-batch-approval.md`, `version.json` (4.19.0 → 4.20.0), `dashboard.md` (META bump), `ship-plan-3` (T1 ✅). MINOR per SemVer (behavior change to an existing command's approval mechanics; no breaking change — [M] preserves the old walk). No DEC; no sync-manifest change (`.claude/commands/*.md` glob auto-covers).

- **v4.21.0 (2026-06-11)** — **`/health-check` batch fix triage (Plan 3 T2)** from the 2026-06-10 cross-repo analysis (root `ship-plan-3-interaction-tax-and-queue.md § T2`). **Evidence basis:** health-check.md carried 23 menus with nested per-diff/per-file prompts (Part 2d action menu, Part 5 "Apply all / Select individually" + per-file [A]/[K]/[D], Part 5c [M]/[S], Part 5d [Y]/[N]+[F]/[C]/[S], plus the central Step 4 "present options and apply immediately before moving to next") — one run could demand 10+ responses against a short-session, pause-disciplined usage profile. **What shipped (all in `commands/health-check.md`):** (1) New top-level **`Fix Queue Protocol (collect, don't prompt)`** section — Parts 1–7 never prompt inline; every fixable issue queues an item (`id · part · file · one-line fix · risk`); risk taxonomy: `—` (safe to bundle), `⚠ overwrites local` (ALL sync-category applies + Part 2a revert — excluded from bare `[A]`, explicit inclusion by id required; sidecar hash-verified rows annotated `(hash-verified: no local edits)` but still ⚠, per the plan's literal "never bundled into bare [A]" reading), `⚠ unreviewed append` (Part 7 FB-entry appends — full text via `[D]`), `needs-input` (question + choices inline on the row; resolved only via per-item reply, never silently dropped). Exceptions preserved: Part 8 audit-dispatch menu stays interactive (gates expensive audits, not fixes — per plan), and intrinsic sub-flows of included rows (Part 2d `[V]` per-section adjudication, Part 2a `merge`) stay interactive post-batch. (2) **Step 4 rewritten** "Offer Fixes" → "Batch Fix Triage": one consolidated table + one response — `[A]` all unflagged · `A except 3,5` · `A include 3,4` (⚠ only by explicit id) · `[N]` none · `[D] 3,6` full diffs/text first without consuming the response · per-item answers combine freely ("A include 4, 5: defer, 7: on-hold"); apply mechanics: part order, dashboard regen deduped to at-most-once-last, per-fix failures don't abort the batch, post-apply summary lists applied/failed/still-open. (3) **Per-part conversions:** Part 2a revert/keep/merge → needs-input row; Part 2b missing-root-CLAUDE.md offer + per-section condensation options → queued rows; Part 2d [V]/[S]/[D] menu → queued offer (`{id}: defer` writes the 30-day sentinel); Part 1 + Part 3 auto-fix tables get a governing line ("Ask user" entries become needs-input rows); Part 5 Step 3 → report + one ⚠ row per sync file (exclusion = old "Keep current" semantics, sidecar untouched, resurfaces next sync; [D] = old per-file diff print incl. ~100-line truncation); Part 5 Step 4 post-sync dashboard [Y/N] → deterministic regen folded into the included row (consequence noted in the row text pre-response); Part 5c [M]/[S] → safe row (content moved, not lost); Part 5d both prompts → needs-input rows (path reply / `{id}: clear`); Edge Cases line updated. **Deviation from plan (scope addition, recorded):** Part 7's per-entry FB-append confirmations (steps 3c + 6) were NOT in the plan's cited sites but are converted to `⚠ unreviewed append` rows — rationale: the imminent T4 insights run over the 47-export backlog would otherwise spam one prompt per bridged item + per high-confidence pattern, exactly the tax T2 targets; the "never append silently" rule is preserved via visible row + explicit inclusion + `[D]` full text. **Design note:** the v3.15.0 sidecar's per-file default-APPLY keystroke is superseded inside `/health-check` by the batch table (hash-verified classification surfaces as row annotation instead); the sidecar's classification/lifecycle machinery is unchanged. **Trace test:** new `tests/scenarios/39-health-check-batch-triage.md` (one-prompt-per-run / bare-[A] ⚠+needs-input exclusion / combined single-response inclusion+answers / Part 8 + --report unaffected). **Files touched (6):** `commands/health-check.md` (13 sites), new `tests/scenarios/39-health-check-batch-triage.md`, `version.json` (4.20.0 → 4.21.0), `dashboard.md` (META bump), `ship-plan-3` (T2 ✅). MINOR per SemVer (behavior change to fix-application mechanics across Parts 1–7; no information loss — report content unchanged). No DEC; no sync-manifest change (`.claude/commands/*.md` glob auto-covers).

- **v4.21.1 (2026-06-11)** — **Handoff schema cap (Plan 3 T4a)** from the 2026-06-10 cross-repo analysis (root `ship-plan-3-interaction-tax-and-queue.md § T4`). **Evidence basis:** styler's `.handoff.json` was a 7.8KB hand-curated free-prose blob — write-expensive, read-unreliable, and (pre-T3) the sole home of two unanswered blocking decisions. **What shipped (all in `support/reference/context-transitions.md`; `commands/work.md § Context Transition` is already a stub delegating here — untouched, per plan):** (1) § Schema example updated — `session_knowledge` becomes an ARRAY of bullet strings; new optional top-level fields `open_question_refs` (pointer strings to the dashboard Action Required rows the T3/v4.14.0 pause sweep writes — pointers only, rows hold the questions) and `overflow_ref` (path to a workspace overflow file, null when everything fits); framing paragraph added ("bounded index, not a memoir", target ≤ ~2.5KB total). (2) New **§ Bounds and Overflow** — per-field cap table (`phase_context` 1–2 sentences (existing); `partial_notes` ≤ ~6 sentences or DEC-010 envelope; `open_question_refs` pointers-only; `session_knowledge` ≤ ~10 bullets × ≤ ~25 words; `recovery_action` ≤ ~3 sentences) + overflow procedure (excess → `.claude/support/workspace/handoff-overflow-{YYYY-MM-DD}.md` organized by field name, `overflow_ref` set, most load-bearing content stays inline; never created preemptively). (3) Field-definition updates for `session_knowledge` (array shape + bounds; **legacy string form still parses** — additive union, explicitly same convention as `partial_notes`' DEC-010 union; schema `version` stays 1) and `recovery_action` (bound inline). **Trace test:** new `tests/scenarios/40-handoff-schema-cap.md` (rich-session overflow / typical-session no-ceremony + legacy-parse). **Files touched (5):** `support/reference/context-transitions.md` (3 sites), new `tests/scenarios/40-handoff-schema-cap.md`, `version.json` (4.21.0 → 4.21.1), `dashboard.md` (META bump), `ship-plan-3` (T4a ✅). PATCH per SemVer (bounds/threshold guidance on an existing artifact; additive optional fields; no interface break — old handoffs parse, readers unchanged). No DEC; no sync-manifest change (file already in `sync`). T4b (insights backlog run + cadence note) is a maintenance action in the template repo, logged separately below — not a template ship.

- **Maintenance run 2026-06-11 (no version bump)** — **Plan 3 T4b: insights backlog processed** (root `ship-plan-3-interaction-tax-and-queue.md § T4`; first pipeline-stage-4 run since insights/ was created 2026-03-30). `/health-check` executed in the template repo under the fresh v4.21.0 batch-triage protocol (dogfood pass). **Part 7:** 39 session exports processed and moved to `processed/` — 20 visible + **19 dot-prefixed exports that were invisible to plain `ls`** (root cause: `context-transitions.md § Session Export` step 6 copies the `.session-export-*` WORKING filename to `template_inbox_path` verbatim, leading dot included; renamed on move to `session-export-{date}-{time}-{project}.json`; fix row queued). Corpus after move: 65 files in processed/, 1 user_feedback file held in inbox pending its queued FB append. **Aggregation spanned all 70 corpus files** (64 session exports + 6 user_feedback; projects: styler 41, echothread 8, flirty-gym/Flirty 10, OEMMatInsightBI 2, tinder-streamliner-cc, SIREN, Personal; 156 automated markers + 197 friction notes + 108 pushback notes). **7 insight documents written** to `interaction-logs/insights/`: (1) files-affected ripple (40+ occurrences, 4 projects — already covered by FB-058 pre-pass + FB-086 v4.8.0 auto-union; version-skew caveat: most evidence predates v4.8.0; no new FB), (2) TaskCreate reminder noise (~22 sessions — FB-075 evidence accumulation, no new FB), (3) dashboard regen cost at scale (~18 notes — demand-side evidence for FB-011 Family C full port; also surfaced the `fingerprint.py --dashboard-rollup` vs dashboard-META task_hash algorithm mismatch to fold into the port), (4) structural-green/runtime-broken (~14 occurrences — **FB-076 re-assess condition (a) "2nd project signals same pattern" arguably MET via echothread**, with a hold-until-post-v4.15.0-exports caveat), (5) friction-persistence bookkeeping weight (5 occurrences, 2 projects → **FB-098 proposed**: persist-friction.py helper), (6) gitignored-.claude state hazard (5 occurrences → **FB-099 proposed**: /health-check untracked-state warning + Step 0e scope fix), (7) owner:both completion path (4 occurrences, 2 projects → **FB-100 proposed**: Step 1d predicate + completion shape; visibility half already shipped v4.14.0). **Step 3c:** flirty-gym bridged item (spec acceptance-criteria boxes never reconciled with phase verification; touches DEC-016 carveout boundary) → **FB-097 proposed** (dedup-checked clean across all four feedback files). All four FB appends queued as ⚠ unreviewed-append rows per the v4.21.0 protocol — NOT appended pending Erik's triage response. **Other parts:** 1/2a/2c/2d/3/4/5c/6 pass or template-intentional fixture state (zero task files + example dashboard documented in root CLAUDE.md; root CLAUDE.md 109 lines = 9 over soft limit, reported not queued — maintenance context file, recently dieted); Parts 5/5d skipped (template repo); Part 5b found a REAL stale list (template-command set missing `shakedown.md` → would false-flag it as custom; fix row queued); Part 8 deferred to Erik (gates expensive audits). **Cadence note added** to `interaction-logs/README.md` (§ Processing Cadence: ~15 exports or monthly, whichever first; aggregate across processed/ with version-skew caveats). **Files touched:** 40 export moves/renames, 7 new insight docs, `interaction-logs/README.md`, this entry, `ship-plan-3` (T4 fully ✅ — file deleted in follow-up housekeeping commit). Template content untouched → no version bump (queued fix rows land with their own bumps once approved).

- **v4.21.2 (2026-06-11)** — **Health-check-run fix pair** (PATCH, no FB — rows 1–2 of the 2026-06-11 batch triage table, approved by user response "do the fixes"). **(1) Part 5b stale template-command set:** `commands/health-check.md § Part 5b` known set was missing `shakedown.md` (shipped via DEC-019) — the collision detector would have flagged `/shakedown` as a custom command with functional-overlap warnings in every downstream project. Added to the set (now 14 entries, matching `.claude/commands/` reality). Found by this repo's own `/health-check` run — the check caught its own definition's staleness. **(2) Session-export dot-filename copy bug:** `support/reference/context-transitions.md § Session Export` step 6 said only "copy the export there", so the dot-prefixed WORKING filename (`.session-export-YYYY-MM-DD-HHMM.json`, step 5) landed in the template inbox verbatim — invisible to plain `ls`. Observed cost: 19 exports silently accumulated unseen (discovered during the 2026-06-11 Part 7 backlog run; renamed on move to processed/). Step 6 now mandates rename-on-copy to `{project-slug}-session-export-YYYY-MM-DD-HHMM.json` (slug from `source_project`) and explicitly extends the rule to every inbox copy path — `work.md` Step 0f recovery exports (which cite "parallel to Session Export step 6" and inherit the rule) and PreCompact markers-only exports. `work.md` untouched (stub discipline — the procedure file is the contract). **Files touched (5):** `commands/health-check.md`, `support/reference/context-transitions.md`, `version.json` (4.21.1 → 4.21.2), `dashboard.md` (META bump), ship-log. PATCH per SemVer (stale-list correction + bug fix; no interface change). No DEC; no sync-manifest change; no new trace test (consistency-fix class, v4.13.1 precedent — the rename contract is stated inline at the canonical procedure site).

- **v4.21.3 (2026-06-11)** — **FB-096 sub-issue A: capability-doc model-surface section verified-then-fixed** (PATCH; Part 2d `[V]` discipline, DEC-017/FB-094 precedent; user-approved "go" on the staged proposal). **Verification:** WebFetched `code.claude.com/docs/en/sub-agents` and diffed against `claude-code-authoring.md § "Agent tool model parameter granularity"` — live-harness observation (this session's Agent tool schema showing `fable`) was corroborating, NOT the sole evidence. **Drift found broader than FB-096's capture:** the old section claimed "only `sonnet | opus | haiku`", "no model-version specificity", "no per-call effort control" — docs confirm (1) alias set is `sonnet|opus|haiku|fable`, (2) **full model IDs accepted** (e.g., `claude-opus-4-8`; "same values as the `--model` flag"), (3) `inherit` is the default, (4) per-invocation resolution order `CLAUDE_CODE_SUBAGENT_MODEL` env → invocation `model` param → frontmatter `model:` → main-conversation model, (5) agent definitions take `effort:` frontmatter (`low|medium|high|xhigh|max`, model-dependent) overriding session effort — so the old Implication ("authors cannot vary effort per dispatch") was wrong for custom-agent dispatches and stands only for ad-hoc dispatches. Section rewritten with all five facts + honest provenance note that the template's `"opus[1m]"` alias+modifier dispatch value is harness-observed working but not docs-enumerated. **Footer** bumped to `@ 2026-06-11; against template_version: 4.21.3` — scope note: this was a TARGETED single-section verification (the drifted section), not a full-doc pass; remaining sections were last fully verified 2026-05-27 (15 days prior, within all Part 2d thresholds either way). **Ledger:** FB-096 entry status → sub-issue A RESOLVED with resolution note (incl. the facts feeding sub-issue B: explicit-pin option (b) confirmed feasible); root `CLAUDE.md` FB-096 line updated. Sub-issue B (pin decision: ratify float / pin explicit / move forward) remains open — Erik's call, now decision-ready. **Files touched (6):** `support/reference/claude-code-authoring.md` (section + footer), `template-maintenance/feedback.md` (FB-096 status + resolution note), root `CLAUDE.md` (FB-096 line), `version.json` (4.21.2 → 4.21.3), `dashboard.md` (META bump), ship-log. PATCH per SemVer (doc correction, no behavior change). No DEC; no sync-manifest change; no trace test (capability-doc content fix — Part 2d's own `[V]` flow is the verification mechanism, scenario coverage unchanged).

- **v4.21.4 (2026-06-11)** — **FB-096 sub-issue B: Opus pin float RATIFIED — option (a)** (PATCH; user decision 2026-06-11 after sub-issue A's v4.21.3 verification made the choice fact-grounded). **Decision basis:** the `opus[1m]` alias has governed every real dispatch since v4.13.1 documented the de-facto split (alias governs at dispatch; pin recorded design target), with zero observed agent regressions across the 70-export corpus (downstream sessions spanning v3.5.1 → v4.11.0) — the 4.7 pin documented a fiction; explicit-pin remains available as the regression escape hatch and is now CONFIRMED feasible (full model IDs docs-verified in v4.21.3). **What shipped (9 edits, 7 files):** (1) `.claude/CLAUDE.md § Model Requirement` (canonical) — restated as targeting the **current Claude Opus tier** via the floating `opus[1m]` alias (ratification dated + FB-096 cited; "originally designed and validated on Opus 4.7" kept as history); divergence-handling rule replaced by the **regression escape hatch**: on post-release agent regressions, pin dispatches to the last-known-good explicit full model ID via deliberate template change and record pin + reason in the section. Dispatched value UNCHANGED (`"opus[1m]"`). (2) `shared-definitions.md` difficulty-calibration note — genericized to current-Opus-tier with validated-on-4.7 history; **spot-check passed** per option (a)'s requirement: the calibration assumptions (adaptive thinking, interleaved reasoning, sustained agentic execution) hold at 4.8/Fable — strictly newer Opus-tier capability supersets. (3) `.claude/README.md` line 5 design-target + line 86 "Recommended setup for Max + Opus" genericized. (4) Agent-file capability prose ×4 (implement ×2, verify, research) — "Opus 4.7's adaptive/interleaved thinking" → "the Opus tier's …". (5) `automation.md` CLI example `--model claude-opus-4-7` → `claude-opus-4-8` (explicit ID remains the right shape for automation/CI; ID bumped to the docs-cited current example). **Left as-is (out of scope, flagged):** `.claude/README.md` line 78's auto-mode availability claim ("available on Max, Team, Enterprise, and API plans with Opus 4.7") — a platform-capability claim, not pin prose; verify in the next full Part 2d-class pass rather than rewriting from inference. **Ledger:** FB-096 fully resolved → full entry moved to `template-maintenance/feedback-archive.md` with both resolution notes (A: v4.21.3; B: this ship); stub left in `feedback.md`; root `CLAUDE.md` Active Follow-ups bullet → italic resolved note (FB-085 pattern). **Files touched (12):** `.claude/CLAUDE.md`, `.claude/README.md` (2 sites), `agents/implement-agent.md` (2 sites), `agents/verify-agent.md`, `agents/research-agent.md`, `support/reference/shared-definitions.md`, `support/reference/automation.md`, `version.json` (4.21.3 → 4.21.4), `dashboard.md` (META bump), `template-maintenance/feedback.md` (stub), `template-maintenance/feedback-archive.md` (full entry), root `CLAUDE.md`. PATCH per SemVer (policy/documentation alignment with de-facto behavior; zero dispatched-value change — v4.13.1 precedent). No DEC (FB-096's own triage: option (a)/(b) are decision-gated only; the FB entry + this ship-log record the decision). No sync-manifest change; no trace test (no behavioral surface change).

- **v4.22.0 (2026-06-11)** — **Family C FULL PORT: script-first dashboard regeneration** (FB-011; user decision "go" on the same-day PoC evidence run recorded in `scripts-candidates.md § Family C`). **Evidence basis:** PoC v4.19.0 + the 2026-06-11 styler evidence run (218 active + 620 archived tasks): LLM render dropped DEC-072 deps from two rows; archive blindness + phase-name divergence identified as the real port scope; three-way `task_hash` mismatch (META ≠ documented algorithm ≠ `fingerprint.py --dashboard-rollup`); demand side = ~18 friction notes showing prescribed regens structurally bypassed at scale (insight doc 2026-06-11). **What shipped:** (1) **`scripts/dashboard-render.py` rewritten** (~215 → ~780 lines, stdlib-only, read-only): `--render` emits the complete dashboard — META (13 whitelisted fields incl. the **canonical `task_hash`**: sha256 over sorted `id:status:difficulty:owner` rows, newline-joined + trailing newline, active tasks only), section-toggle checklist from sidecar, header/completion/freshness lines, 📊 Progress (status summary >20 gate, archive-aware phase table with Complete/Active/Partially Actionable/Blocked statuses, acceptance criteria incl. summary-only fallback, timeline with overdue strikethrough, **critical-path one-liner** (longest-path graph walk, decision nodes, bounded fork/join bracketing, >5-step truncation, all documented edge cases), **Project Overview Mermaid** (completed-phase P-nodes emitted lazily — only when edges reference them; finished-task fold-and-reroute; >15 active nodes → critical-path-only mode + omitted note; <4 → omit; decision diamonds unclassed per golden example), this-week + recent-activity from date approximations), 📋 Tasks (archive-aware), 📋 Decisions (status mapping + checked-option parse + doc links), Notes preserved verbatim from sidecar `user_notes`, Custom Views instruction markers, footer with health indicator; synthesis sections (🚨 Action Required, Custom Views rendered content) emit **`<!-- CLAUDE: fill … -->` placeholders** — the LLM's remaining regen duty. `--tasks-section` kept (now archive-aware; byte-identical when no archive exists — original 13 tests untouched and green); new `--task-hash` mode = the single hash authority; `--now <ISO>` for deterministic output. **Evidence findings all addressed:** archive input reads full `archive/task-*.json` with `archive-index.json` count-fidelity fallback; archived Finished count into done/total, archived Absorbed excluded, archived other-status excluded + surfaced as "(+N archived non-finished)"; phase names = union vote across active+archive (deterministic tie-break); absorbed-only phases collapse naturally under archive-aware counting; "Phase None" → "### Unphased". **Validated:** 54/54 tests (29 new: archive-awareness ×5, hash convention ×2, decisions ×1, progress ×4, critical path ×8, mermaid ×3, full render ×5, CLI ×1) + byte-identical full re-render on styler's real corpus + zero-task template-repo edge. **Documented simplifications** (script docstring): no Mermaid subgraph clumping (straight to critical-path-only), no phase-gate hexagons (Action Required carries gates), bounded fork/join detection, date-approximated activity, deterministic count-enumeration qualifiers. **(2) Wiring:** `dashboard-regeneration.md` — new § "Script-First Rendering" (division-of-labor table; Step-2-first ordering — script reads user content from the SIDECAR; script-is-authoritative-on-divergence rule; canonical-hash warning against `fingerprint.py --dashboard-rollup`; prose = complete hand-render fallback), Step 3 script-first preamble, Step 8 check 4 (no unfilled placeholders); `rules/dashboard.md § Regeneration Strategy` script-first paragraph; `scripts/README.md` row + status (advisory → WIRED); `commands/health-check.md` Part 1 check 10 → `--task-hash` canonical pointer. Targeted-edit lite path (FB-080) explicitly unchanged. **Trace test:** new `tests/scenarios/41-script-first-dashboard-regen.md` (script-then-fill + placeholder gate / hash authority / fallback + targeted-edit preservation). **Ledger:** Family C CLOSED in `scripts-candidates.md` (+ shipped note) and root `CLAUDE.md` FB-011 line (incl. Family D note: the T729/T731 collision was an input-data miss, trigger stands unmet). **Files touched (12):** `scripts/dashboard-render.py`, `scripts/tests/test_dashboard_render.py`, `support/reference/dashboard-regeneration.md` (3 sites), `rules/dashboard.md`, `scripts/README.md`, `commands/health-check.md`, new `tests/scenarios/41`, `template-maintenance/scripts-candidates.md`, root `CLAUDE.md`, `version.json` (4.21.4 → 4.22.0), `dashboard.md` (META bump), ship-log. MINOR per SemVer (regeneration behavior changes to script-first; prose fallback preserves the old path; no breaking interface change). No DEC (FB-011 special-case tracker + evidence run + user go = the decision record per Family C's established convention). No sync-manifest change (all files in existing globs/entries).

- **v4.23.0 (2026-06-12)** — **3-FB `/work`-family bundle from a `/feedback review` of the maintenance queue** (FB-098 + FB-099 + FB-100; insights-derived items from the 2026-06-11 stage-4 run; v4.8.0 5-FB-bundle precedent). **FB-098** — new `.claude/scripts/persist-friction.py` (stdlib-only, **read-only** per the scripts contract) mechanizes the friction-marker bookkeeping in `work-procedures.md § "State Persistence Protocol"` step 2: takes a marker batch (stdin JSON), reads the existing `friction.jsonl` + optional `--scan` paths, emits the dual-write `markers` (orchestrator appends to BOTH `.pending-markers.jsonl` AND `.session-log.jsonl`), the audit-register `register` projection (audit-eligible kinds only; `source_anchor` required or it warns + skips that projection), **collision-safe `FR-NNN` ids** (one past the max of register `id`s AND textual `FR-<n>` references in the scan paths — fixes the naive-max+1 collisions: flirty-gym FR-001, styler FR-031), `assigned_ids`, and `warnings`. 11 new tests (`scripts/tests/test_persist_friction.py`); full suite 65/65 green; live smoke-test confirmed (audit-eligible marker → FR-001, template-only kind not projected). Advisory wiring added to `work-procedures.md` step 2 + a row in `scripts/README.md`; prose remains source of truth (dual-location discipline). Direct template edit per Family A/B precedent — recorded against the parked Family F checker in `scripts-candidates.md` (helper makes compliance cheap; Family F still parked as the non-compliance checker — the helper does not satisfy its trigger). **FB-099** — gitignored-`.claude/`-state hazard surfaced two ways: (a) `/work` **Step 0e** (Uncommitted-Work Check) `.claude/`-exclusion **scoped to gitignored paths only** — the old blanket filter hid git-*tracked* `.claude/vision/*.md` / command / task-JSON edits; now relies on git's native gitignore semantics (tracked-diff lists only tracked files, `--exclude-standard` honors `.gitignore`), so gitignored state stays out while tracked `.claude/` source counts (rationale rewritten); (b) new `/health-check` **Part 4 check 5** (informational) — `git check-ignore` against `spec_v*.md` / `tasks/` / `support/decisions/`; if any are ignored, reports once ("source-of-truth state is untracked — deliberate? consider a backup convention"), never blocks, respects deliberate fork conventions; uses `check-ignore` (reliable exit codes) not a `.gitignore` grep. Distinct from FB-063 (worktree reads of gitignored state). **FB-100** — `owner: "both"` routing + completion: (a) `/work` **Step 1d** fast-path predicate extended with `owner == "both" AND user_review_pending == true` (a both-owned task is non-actionable once Claude's half is delivered and it's awaiting the user) + a modeling note that both-tasks gated on a physical-world prerequisite *before Claude can act* should carry `Blocked`/`On Hold` so the existing clauses catch them; (b) `work-procedures.md § "Task Completion"` documents the both-owned **completion shape** — Claude's half verified the normal way; the user's half (or a lived/conversational gate with nothing mechanical to verify) closes via the same `self_attested` block as `owner: "human"`, no verify-agent round-trip. Builds on v4.14.0's waiting-on-you visibility half. **Files touched (13):** new `.claude/scripts/persist-friction.py` + `.claude/scripts/tests/test_persist_friction.py`, `.claude/scripts/README.md`, `.claude/commands/work.md` (Step 0e item 4 + rationale, Step 1d predicate + note), `.claude/commands/health-check.md` (Part 4.5), `.claude/support/reference/work-procedures.md` (State Persistence step 2 advisory note + Task Completion owner:both bullet), `.claude/version.json` (4.22.0 → 4.23.0), `.claude/dashboard.md` (META bump), `template-maintenance/{feedback.md (3 stubs), feedback-archive.md (3 full entries), scripts-candidates.md (Family F complement note)}`, root `CLAUDE.md` (Active Follow-ups digest), ship-log. MINOR per SemVer (new script + Step 1d routing behavior change; the Step 0e filter refinement and both-completion doc are additive — no breaking change). No DEC (all three carried direct-edit dispositions; **FB-097** — the 4th new item, a DEC-016-carveout-amendment candidate for spec acceptance-box reconciliation — stays `new`, research-gated, untouched this ship). No sync-manifest change (`.claude/scripts/*` + `commands/*.md` + `support/reference/*.md` already covered by existing globs/entries). No trace test (the script's unit suite is the verification surface; the command edits are behavioral refinements with prose-traceable logic).

- **v4.24.0 (2026-06-12)** — **DEC-021: spec-scale architecture → Option 2 (single file + generated section index + section-scoped read discipline).** Resolves **FB-095** (spec-scale ceiling — styler `spec_v15.md` at 846K chars / 57 `## Phase` sections / largest `## Phase 40` = 58K). Chosen via `/research` over sharding (Opt 1) / tiering (Opt 3) / diet (Opt 4) on a **blast-radius asymmetry**: Option 2 preserves the "exactly one `spec_v{N}.md`" Critical Invariant, leaves `fingerprint.py` + all task/decision provenance untouched, and is a derived/deletable artifact (reversible) — vs. Opt 1/3 breaking the invariant + migrating all task provenance. Targets the operative pain (consumption, not storage); matches the template's evidence-first/reversible-default posture (DEC-018, FB-085). Record `decisions/decision-021-spec-scale-architecture.md` (status `implemented`, 7 anchors); research archive `.archive/decision-021-research-2026-06-12.md` (25-row blast-radius table + drift deep-dive + migration analysis; the research-agent read styler's live spec to ground the figures). **What shipped:** (1) **`fingerprint.py --index`** — emits the spec section index (per-`## ` heading → 1-based `line_start`/`line_end` → `fingerprint` → deterministic `synopsis`, + top-level `spec_fingerprint` for the freshness check); per-section fingerprints are byte-identical to `--sections` so the index and the drift map stay consistent. (2) **Companion (a): `--sections --depth 3`** — additive `### ` subsection hashes; the `## ` hashes are UNCHANGED at any depth, so opting in causes **zero downstream drift churn**. Addresses already-coarse fingerprinting at scale (a one-line edit in the 58K `## Phase 40` currently rehashes 58K). (3) **Canonical discipline:** new `rules/spec-workflow.md § "Section-scoped spec reading"` (auto-loaded) — consult index → `Read offset/limit` the 1–3 relevant sections; "scoped by default, whole when warranted"; explicitly a convention preserving the single-file invariant. (4) **Freshness:** `drift-reconciliation.md § "Spec Index Freshness"` (regenerate when missing or `spec_fingerprint` mismatches; carries no task provenance → never a correctness risk) + `work.md` Step 1b proactive regen (reuses the fingerprint it already computes) + `If Decomposing` index-generation note. (5) **Subagent pointers:** `implement-agent.md` + `verify-agent.md` get explicit scoped-read instructions (subagents don't auto-load the rules files). (6) **Registrations:** `paths.md` (row + note), `.claude/CLAUDE.md` Navigation row, `scripts/README.md` fingerprint row. **Untouched** (the asymmetry payoff): the Critical Invariant, `settings.json` DEC-016 globs, `drift-reconciliation` core, all task/decision provenance — `fingerprint.py` only GAINS modes (existing `--spec`/`--sections`/`--dashboard-rollup` byte-identical). **Validated:** 70/70 script tests (5 new: index line-ranges, index↔sections fingerprint consistency, synopsis-skips-subheadings, depth-3 additivity, depth validation) + live `--index`/`--depth` smoke. Trace test `tests/scenarios/42-section-scoped-spec-reading.md`. **Files touched (13 implementation):** `scripts/fingerprint.py`, `scripts/tests/test_fingerprint.py`, `scripts/README.md`, `rules/spec-workflow.md`, `support/reference/drift-reconciliation.md`, `support/reference/paths.md`, `.claude/CLAUDE.md` (Navigation), `commands/work.md` (Step 1b + decomposition note), `agents/implement-agent.md`, `agents/verify-agent.md`, `version.json` (4.23.0 → 4.24.0), `dashboard.md` (META), new `tests/scenarios/42-section-scoped-spec-reading.md`. **Ledger:** `decisions/decision-021-spec-scale-architecture.md` (→ `implemented` + anchors), `decisions/.archive/decision-021-research-2026-06-12.md` (from the `/research` step), `template-maintenance/feedback.md` (FB-095 → promoted stub) + `feedback-archive.md` (FB-095 full entry), root `CLAUDE.md` (follow-up → resolved). MINOR per SemVer (new script modes + new behavior surface; the discipline is additive — `--sections` default, the invariant, and all provenance are unchanged → no breaking change). DEC-021 is the decision record. No sync-manifest change (`scripts/*`, `commands/*.md`, `rules/*.md`, `agents/*.md`, `support/reference/*` already covered). **FB-095 → promoted.** Companion follow-up still available: wiring drift-reconciliation to *consume* `### ` granularity (the script now emits it; the consumer adopting it is a separate, optional future step). *(Closed in v4.25.0 — see below.)*

- **v4.25.0 (2026-06-12)** — **DEC-021 companion follow-up: drift-reconciliation now *consumes* the `### ` granularity** (closes the v4.24.0 loose end). The `--depth 3` primitive shipped in v4.24.0 emitted `### ` subsection hashes but nothing read them — drift still keyed on `## ` sections, so a one-line edit in styler's 58K `## Phase 40` re-flagged every task under it. **What shipped:** (1) **Subsection-level drift narrowing** — new `drift-reconciliation.md § "Subsection-level drift narrowing"`: when granular analysis flags a *large* changed `## ` section, drift computes the `### ` diff (`fingerprint.py --sections --depth 3`, current vs snapshot), surfaces *which* subsections changed in the Granular Reconciliation UI (diagnostic — sharpens reconciliation even for legacy tasks), and **narrows the auto-flagged task set** so a task in a provably-unchanged subsection is spared. (2) **Optional task provenance** — new optional `spec_subsection` + `subsection_fingerprint` fields (`task-schema.md`); a task carrying them is flagged only when its own subsection changed, else moved to a conservative **"likely unaffected (subsection unchanged)"** group with `[S] Skip` recommended (**surface-don't-drop** — never silently dropped). Tasks **without** the fields (legacy / small-section / whole-section) fall back to `## `-level flagging — **no regression**. (3) **Decomposition** (`decomposition.md` step 8) gains an optional sub-bullet: record `spec_subsection` for tasks scoped to one `### ` of a *large* `## ` section (gauged by the index `char_count`); skip otherwise. (4) **Wiring:** `work.md` Step 1b clause; on-apply updates `subsection_fingerprint`; version-transition migration carries it when present. **Back-compat + reversible by construction:** the `### ` hashes are additive (`## ` hashes unchanged → no false drift), the new fields are optional, and every failure path (no snapshot / no `--depth 3` / small section / no provenance) degrades to today's `## `-level behavior. **Validated:** 70/70 script tests (the additive `--depth 3` primitive is covered by `test_sections_depth3_is_additive`; the narrowing is orchestrator prose — conceptual coverage added as Trace D in `tests/scenarios/42-section-scoped-spec-reading.md`). **Files touched (8 + this ship-log):** `support/reference/drift-reconciliation.md` (narrowing section + on-apply + migration), `support/reference/task-schema.md` (2 optional fields + drift-explanation), `support/reference/decomposition.md` (optional capture), `commands/work.md` (Step 1b clause), `tests/scenarios/42-section-scoped-spec-reading.md` (Trace D), `version.json` (4.24.0 → 4.25.0), `dashboard.md` (META), `decisions/decision-021-spec-scale-architecture.md` (2 anchors + Impact companion note). MINOR per SemVer (new optional schema fields + new drift-narrowing behavior; additive — no breaking change). DEC-021 stays `implemented` — this completes its companion follow-up, so the FB-095/DEC-021 loose end noted in v4.24.0 is now **closed**. No sync-manifest change (`support/reference/*` + `commands/*.md` already covered). No further follow-up required: the companion is fully wired (a future project could extend the substantial-change heuristic to `### ` awareness, but `## `-level there is sufficient).
- **v4.26.0 (2026-06-12)** — **DEC-022: spec acceptance-criteria ↔ phase-verification reconciliation → Option D (A + C).** Resolves **FB-097** (flirty-gym: Phase 2 spec acceptance boxes all `[ ]` despite a recorded 7/7 phase-level PASS). Chosen via `/research` over the FB-097-leaned **Option B** (orchestrator auto-ticks spec boxes) — research **declined B's mechanism**: the `criteria[]`→box mapping is structurally unsafe (free-text, re-segmented, no ID link → fuzzy-match only, DEC-022 Q2); ticking a box changes the section fingerprint, risking Finished→Pending resets (Q5); and "write status into the authored doc" is the pattern RTM/BDD/DOORS deliberately avoid (Q4). Record `decisions/decision-022-acceptance-criteria-reconciliation.md` (status `implemented`, 6 anchors); research archive `.archive/decision-022-research-2026-06-12.md` (Q1–Q7 + external traceability sources). **What shipped — A (doctrine):** new `rules/spec-workflow.md § "Acceptance-criteria authority (DEC-022)"` — phase acceptance *status* authority = `.claude/verification-result.json` `criteria[]` (rendered as the dashboard `### Acceptance Criteria`); inline spec `- [ ]` boxes are **authored input, not a live status field** (an optional project convention the template never mandated — Q3); the spec stays source of truth for the *criteria themselves*. Coherent with "source of truth, *or updated intentionally*" (Q6) and mirrors the per-task layer where `task_verification.result` is the structural gate (Q7). Authority notes added at the render site (`dashboard-regeneration.md` § Acceptance Criteria), `health-check.md` Part 1, and a new `.claude/CLAUDE.md` Critical Invariant. **C (lens):** new 7th `/audit-coherence` lens `acceptance-reconciliation` — advisory; flags spec inline-box vs `criteria[]` divergence per phase (the flirty-gym symptom), `kind: decision` so reconciliation routes via `/iterate`; the fuzzy box↔criterion match is advisory-only and **never mutates the spec** (sidesteps the Q2 risk that disqualified B); returns 0 findings when the spec renders no inline boxes (the common case). Capture adds `inputs/verification-result.json`; all lens-count / prefix / file-layout scaffolding updated (6 → 7). **Why A+C not B+C:** C delivers B's intent (keep the doc honest) without B's mechanism, routing the actual fix through `/iterate` where DEC-016 puts spec-body edits. **Files touched (6 implementation):** `rules/spec-workflow.md`, `support/reference/dashboard-regeneration.md`, `commands/health-check.md`, `.claude/CLAUDE.md` (Critical Invariant), `commands/audit-coherence.md` (7th lens + scaffold), `version.json` (4.25.0 → 4.26.0). **Ledger:** `decisions/decision-022-*.md` (→ `implemented` + 6 anchors), `decisions/.archive/decision-022-research-2026-06-12.md` (from the `/research` step), `template-maintenance/feedback.md` (FB-097 → promoted stub) + `feedback-archive.md` (FB-097 full entry + resolution note), root `CLAUDE.md` (follow-up → resolved). **Zero edits** to `verify-agent.md`, `work.md` completion flow, `settings.json`, or `drift-reconciliation.md` — the economy of A+C over B. MINOR per SemVer (new doctrine + optional advisory lens; additive — no permission-layer or task-schema change, contrast DEC-016's MAJOR). No sync-manifest change (`rules/*.md`, `commands/*.md`, `support/reference/*`, `.claude/CLAUDE.md` already covered). Trace test `tests/scenarios/43-acceptance-reconciliation-lens.md` (4 traces: flag-divergence / box-less-silence / advisory-no-mutation / authority-doctrine + invariant checks). **FB-097 → promoted.**
- **v4.27.0 (2026-06-12)** — **DEC-022 follow-up: full historical reconciliation for the `acceptance-reconciliation` lens.** The v4.26.0 lens reconciled only the *latest* phase (`.claude/verification-result.json` is overwritten each phase), so it missed the flirty-gym symptom once a project advanced past the stale phase. It now reconciles **every completed phase** via two evidence tiers, using only existing persisted state (no verification-model change — the A+C economy held): (1) **authoritative** — the latest phase still uses `verification-result.json`'s real `criteria[]`; (2) **proxy** — earlier completed phases use a new capture artifact `inputs/phase-verification.json`, a per-phase task-verification rollup where `phase_complete` = all non-Absorbed/non-On-Hold tasks Finished+verified (the phase gate's own completion condition). Unticked boxes for a `phase_complete` phase are flagged (labeled "(proxy)"); the in-progress phase is never flagged. Residual (honest): the proxy is a per-task signal, marginally weaker than a persisted phase-level `criteria[]` — adequate for an advisory detector that routes to `/iterate`. **Files touched (4):** `commands/audit-coherence.md` (capture step 9 rollup + file-layout + lens-table row + two-tier Lens 7 prompt), `version.json` (4.26.0 → 4.27.0), `dashboard.md` (META), `tests/scenarios/43-acceptance-reconciliation-lens.md` (Trace E historical-coverage + Coverage-tiers section). DEC-022 stays `implemented` (Impact gains a v4.27.0 follow-up note). MINOR per SemVer (new lens behavior + new capture artifact; additive — no verification-model or task-schema change). No sync-manifest change (`commands/*.md` covered). **The bigger alternative — persisting per-phase verification history for authoritative historical `criteria[]` — remains a separate future DEC, only if the proxy proves insufficient.**

- **v4.28.0 (2026-06-22)** — **DEC-023 Phase 0: vision-as-development-hub structure.** First phase of the spec-shaping workflow redesign (`decisions/decision-023-vision-hub-and-spec-shaping-workflow.md`, approved 2026-06-22; analysis substrate `template-maintenance/command-workflow-analysis.md` § 1–12). The redesign came out of a `/feedback review` (FB-101/FB-102) that opened into the broader "commands don't compose smoothly" ask; the diagnosis: the build loop is well-orchestrated but the spec-shaping front-end (grill → shakedown → iterate) is loosely coupled and the **vision** has no structure despite being the user's de-facto recurring feature-development hub. Phase 0 formalizes that hub. **What shipped:** (1) **new scaffold** `.claude/vision/_feature-vision-template.md` — reverse-engineered from styler's ~11 visions: immutable header (`status`/`supersedes`/scope) + thesis + `§`-sections each with a **maturity banner** (🟡 DRAFTED → 🔵 RESEARCHED → 🟢 SHIPPED, evidence-cited) + **one canonical Open-forks tracker** (fork · thread · `[structure]`/`[calibration]` · probe tool · status — fixes the scattered-forks awkwardness) + status map + evidence index (cite-not-copy) + **structured Amendments table** (replaces prose blocks); `_`-prefixed files are scaffolds, skipped by consumers. (2) `rules/spec-workflow.md § "Vision Documents"` rewritten to the **hub model** — recurring per-feature workspace, lifecycle (capture → develop via grill/shakedown passes → graduate to spec), and **target-awareness** (grill/shakedown target vision · spec · build — *the target sets the altitude*). (3) **DEC-016 vision-portion amendment** — a vision is **editable in-place during development (🟡/🔵), frozen after graduation to spec (🟢)**; the `permissions.ask` gate is **kept** as a per-session confirm (user's call) but no longer implies `/iterate`-routing for dev-phase vision edits. Applied across four surfaces: `rules/spec-workflow.md § "Direct edits…"` (vision carve-out), `.claude/CLAUDE.md` Critical Invariant, `.claude/vision/README.md` (reframed one-time-input → hub), and a reciprocal amendment banner + `related: DEC-023` in `decisions/decision-016-spec-file-edit-guardrail.md`. **Files touched:** `.claude/vision/_feature-vision-template.md` (new), `.claude/rules/spec-workflow.md`, `.claude/vision/README.md`, `.claude/CLAUDE.md`, `decisions/decision-016-spec-file-edit-guardrail.md`, `decisions/decision-023-vision-hub-and-spec-shaping-workflow.md` (new), `template-maintenance/command-workflow-analysis.md` (new), root `CLAUDE.md` (Active Follow-ups resume pointer), `.claude/sync-manifest.json` (scaffold registered in `sync`), `.claude/version.json` (4.27.0 → 4.28.0). MINOR per SemVer (new scaffold + convention + governance amendment; additive — the gate stays, only the vision *behavioral* rule changed; spec/decision guardrails untouched). DEC-023 stays `approved` (multi-phase build; → `implemented` when Phases A–C land). **Next:** Phase A (merge-queue transport: schema + `support/reference/merge-queue.md`), then B (target-aware grill/shakedown + `/iterate` drain), then C (feedback dispatcher).

- **v4.29.0 (2026-06-22)** — **DEC-023 Phase A + B (minimal): merge-queue transport LIVE.** The re-entry plumbing now works end-to-end — the moment the "I take a `/grill` or `/shakedown` excursion and can't get the output back into the flow" friction (DEC-023 § Background) is actually fixed. **A (contract):** new `.claude/support/reference/merge-queue.md` defines the `.claude/support/.spec-merge-queue.jsonl` register — schema (`id` MQ-NNN · `source` grill/shakedown/feedback · `target` vision/spec · `target_ref` · `origin_ref` · `kind` gap/term/decision/delta · `summary` · `drafted_delta?` · `needs_impact_assessment` · `status` open→merged/dismissed · `created`), write protocol (main-thread producers append; subagents can't per DEC-004), drain protocol, and the **composition with the vision's in-doc Open-forks tracker** (queue = cross-conversation transport + spec-target; fork-tracker = in-doc accumulation — complementary, not redundant). Registered in `.claude/sync-manifest.json` (sync) + `.claude/CLAUDE.md` Navigation. **B (wiring):** (1) `/iterate` new **Step 1c: Drain the Merge Queue** — on entry, surfaces open items grouped by `target_ref` as the re-entry prompt (`[Y]` fold all / `[R]` review each / `[S]` skip), applies by target (`spec` → Step 4 propose-approve-apply; `vision` → in-place fold per the DEC-016 carve-out), marks `merged`/`dismissed`; `needs_impact_assessment` items held back for `/feedback` (G2). (2) `/grill` + `/shakedown` **emit hooks** — surfaced spec/vision findings (grill: terms / forks / changes; shakedown: ⚠ gaps + drafted deltas) append queue entries when the target isn't open in-session, or fold straight into an in-session vision's fork-tracker. **Files touched:** `.claude/support/reference/merge-queue.md` (new), `.claude/commands/iterate.md` (Step 1c), `.claude/commands/grill.md` (routing-to-queue subsection), `.claude/commands/shakedown.md` (emit in Routing onward), `.claude/CLAUDE.md` (Navigation row), `.claude/sync-manifest.json` (register merge-queue.md), `.claude/version.json` (4.28.0 → 4.29.0). MINOR per SemVer (new transport + new command-entry behavior; additive — `/iterate` Step 1c is silent when the queue is empty/absent; the grill/shakedown emit is additive). DEC-023 stays `approved`. **Next:** v4.30.0 = the `/shakedown` deep reframe (purpose-leads + spec-target mode + general-first + extract-more + use-the-user-more — the part Erik cares most about); then Phase C = `/feedback` dispatcher (active grill/shakedown escalation + impact-assessment home).

- **v4.30.0 (2026-06-23)** — **DEC-023 Phase B: the `/shakedown` reframe** (+ `/grill` general-first). Rewrites `/shakedown` around its real purpose and makes it target-aware. **Purpose-leads:** the command now opens by stating a shakedown is *the channel for knowledge only the user has* — edge cases, real incidents, hidden constraints the model can't see (out of training distribution / domain-unique) — and that the auto-verdict on clear cases exists to *clear the deck* so the dialogue concentrates where the user's knowledge is load-bearing (not to race through inputs on thin info). **Target-aware (DEC-023):** point it at a **vision** / **spec** / **build**; *the target sets the altitude* and grounding shifts to match (the spec's described model vs actual code). New Phase-0 step 0 (identify target) + spec/vision grounding; verdict legend gains a spec/vision reading (a ⚠ against a spec *is* a delta). **General-first conduct** (both `/shakedown` Phase 0 and `/grill`'s explicit-topic flow): open conceptual, confirm the frame, *then* descend into repo specifics — fixes the "goes too deep too fast" friction. **Extract-more-per-example:** the probe loop now *generalizes* each example to its family, *drafts the spec/vision delta* a ⚠ implies (a proposal — never written directly, DEC-016), and *seeds an acceptance probe* for a confirmed ✓. **Use-the-user-more:** explicit principle — Claude grounds & proposes, the user adjudicates the judgment calls — auto-verdict clear ✓/✗, but *stop and pull the user in* on flattening calls ("approximating as X loses Y — acceptable?"), gaps (co-draft the delta + rank priority for `/iterate` fold-order), and ambiguity; propose verdicts and let the user override; steering also asks for the cases the user is most worried about. **Files touched:** `.claude/commands/shakedown.md` (wholesale reframe — preserves the v4.29.0 merge-queue emit hook + corpus / out-of-scope / relationship / references sections), `.claude/commands/grill.md` (general-first paragraph in the explicit-topic flow), `.claude/CLAUDE.md` (Environment Commands `/shakedown` one-liner reframe), `.claude/rules/spec-workflow.md § "Workflow Cycle"` (working-backward pointer → target-aware + merge-queue routing), `.claude/version.json` (4.29.0 → 4.30.0). MINOR per SemVer (command behavior/framing upgrade + new spec/vision-target mode; additive — build-target behavior unchanged, the reframe sharpens rather than breaks). DEC-023 stays `approved`. **Next:** Phase C = `/feedback` dispatcher (active grill/shakedown escalation when a triage item exceeds light-clarify + impact-assessment home for returning shakedown-deltas) — the last phase.

- **v4.31.0 (2026-06-23)** — **DEC-023 Phase C: the `/feedback` dispatcher — DEC-023 COMPLETE.** Makes `/feedback` the inbox + dispatcher for the capture/develop/route family (DEC-023 § 9). **What shipped (all in `.claude/commands/feedback.md`):** (1) **lede** reframed — `/feedback` is the dispatcher: when an idea needs more than a light clarify it escalates to `/grill` (meaning) or `/shakedown` (edge-cases / world-knowledge), and it's the impact-assessment home for deltas those route back. (2) **New `[G] Develop` action** in the Phase 2 (Refinement) options menu + an `Action: Develop [G]` subsection documenting the **light-clarify-to-route vs deep-develop boundary** (G1/G3): a 1–2 question clarify stays in `/feedback` (`[R] Refine`); a fuzzy-meaning or edge-case-dependent item escalates to a `/grill`/`/shakedown` excursion (or a feature vision for a larger feature). `/feedback` points to the excursion + sets the item `reviewing`; findings return via the merge queue. (3) **Phase 3 (Impact Assessment) is now also the home for returning deltas (G2)** — it reads `.claude/support/.spec-merge-queue.jsonl` for `needs_impact_assessment: true` items (shakedown/grill deltas `/iterate` held back), assesses them alongside `refined` feedback, and on approval clears the flag so `/iterate`'s drain folds them (on close → `status: dismissed`). Closes the loop opened by `/iterate` Step 1c in v4.29.0. **Files touched:** `.claude/commands/feedback.md` (lede + Phase 2 option + Develop action + Phase 3 merge-queue pickup), `decisions/decision-023-vision-hub-and-spec-shaping-workflow.md` (→ `implemented`, 10 implementation_anchors, Implementation section), root `CLAUDE.md` (Active Follow-ups → DEC-023 complete), `.claude/version.json` (4.30.0 → 4.31.0). MINOR per SemVer (new dispatcher behavior; additive). **DEC-023 is COMPLETE (v4.28.0–v4.31.0):** vision-as-development-hub + target-driven grill/shakedown + merge-queue re-entry transport + `/feedback` dispatcher; amends DEC-016 (vision editable-during-dev / frozen-after-graduation). The spec-shaping front-end now composes — excursions fold back via the merge queue, the vision is a structured development hub, and `/feedback` routes ideas to the right development tool. Net new from the original FB-101/FB-102 `/feedback review` that opened the thread: 1 new scaffold, 1 new reference doc, 4 command upgrades, 1 DEC, 1 DEC amendment, 0 new commands (the DEC-018 minimalism held).
- **v5.0.0 (2026-06-24)** — **MAJOR: DEC-024 — HTML dashboard render target; Markdown hard-retired.** The dashboard is now a single read-only, offline, `file://`-openable **HTML** page (`.claude/dashboard.html`) generated by `dashboard-render.py --html`. Every visualization is **inline SVG rendered in Python at generation time — zero runtime/CDN deps** (the `file://` crux: `fetch()` of siblings and ES-module CDN imports both fail from a double-clicked file, so everything is inline; this also removed the runtime mermaid graph, which rendered flaky/not-at-all under `file://`). **What shipped:** (1) **`dashboard-render.py`** — new `render_full_html()` reusing the existing data layer (never scrapes rendered MD): completion ring + status donut + 53-phase **heatmap** + active-front cards + a **hand-rolled layered-DAG → inline-SVG dependency graph** (over `build_graph`/`longest_path`, owner-colored, critical-path-emphasized, **auto-hidden when degenerate** — <4 incomplete task nodes / no edges / cycle — and reduced to critical-path+neighbors above 15 nodes) + acceptance-criteria status surface (DEC-022) + Timeline + Recent + **collapsed link-out Decisions** (search retained; styler's 141 → one line) + **link-out Specification** card (headings from the index, spec **not embedded**) + read-only Notes card (from sidecar). META block carried verbatim in `<head>`. The 14 **Markdown renderers + the `--render`/`--tasks-section` modes were deleted** (`--html` + `--task-hash` are the only modes); the no-`python3` Markdown fallback is gone. (2) **Tests** — `test_dashboard_render.py` rewritten to the data layer (24) + new `test_dashboard_render_html.py` (24: determinism, META-in-head, placeholders, SVG charts, adaptive graph, acceptance-criteria surface, **no `file://`-breakers**); full suite 76 green. (3) **Docs/consumers** — `rules/dashboard.md` + `support/reference/dashboard-regeneration.md` rewritten for the HTML target (full-regen-only, no targeted-edit path; sidecar is the **sole** user-content source — no in-file markers; SVG "Dependency Graph" replaces the Mermaid "Project Overview"); first-regen detection is now **sidecar-absent** (the template ships no dashboard — it's gitignored/derived); `commands/{work,status,health-check}.md` read the META from `<head>` and health-check's Mermaid/line-count checks became HTML-integrity + byte-size + offline-invariant checks; `agents/{implement,verify}-agent.md` read `dashboard.html` for orientation only; phase-gate approval moved to **CLI + sidecar `phase_gates`** (read-only HTML has no checkbox). (4) **Migration** — `.gitignore` += `.claude/dashboard.html`; `git rm .claude/dashboard.md` (the old tracked example); the 11 downstream dashboards auto-migrate via the `template_version` format-staleness trigger on next `/work` (Step 2c extracts old MD markers → sidecar, deletes `dashboard.md`). **Files touched:** `.claude/scripts/dashboard-render.py` (+ both test files), `.claude/rules/dashboard.md`, `.claude/support/reference/{dashboard-regeneration,phase-decision-gates,workflow,parallel-execution,paths,task-schema,shared-definitions,extension-patterns,work-procedures}.md`, `.claude/scripts/README.md`, `.claude/CLAUDE.md`, `.claude/commands/{work,status,health-check}.md`, `.claude/agents/{implement,verify}-agent.md`, `.claude/sync-manifest.json`, `.gitignore`, `.claude/version.json` (4.31.0 → 5.0.0), root `CLAUDE.md`. Decision record `decisions/decision-024-html-dashboard-render-target.md` (→ `implemented`, 9 implementation_anchors); research archive `decisions/.archive/decision-024-research-2026-06-24.md`. **MAJOR per user call** (SemVer policy default was minor — dashboard isn't a command/task-schema interface — but the user chose MAJOR to signal the retired Markdown surface + removed script modes loudly to downstream). Visual-verified via headless Chrome on SIREN (graph case) + styler (53-phase heatmap / 141-decision scale). **Note:** the DEC-024 record + research repeatedly call the broken-mermaid bug "FB-007" — that is a mislabel (the template's real FB-007 is the spec-edit guardrail, shipped via DEC-016 in v4.0.0; no FB ever tracked the mermaid issue). DEC-024 removes runtime mermaid regardless; this ship makes **no** FB-007 linkage.
- **v5.1.0 (2026-06-24)** — **Dashboard HTML legibility + consistency pass (post-DEC-024 polish).** Origin: an in-session UX review of two **live downstream** dashboards (styler's 867-task / 53-phase board + tinder-streamliner's 56-task board), rendered in headless Chromium and read as a user. Six renderer fixes, all in `dashboard-render.py` (the data layer is untouched): (1) **Task-count consistency** — the header (`<span class="tv">`) + footer now count `total_all` (the completion-ring / phase-map basis = active-non-absorbed + archived-finished), not `len(active)`. On archived projects `len(active)` only counted current task files, contradicting the ring/donut/phase totals — styler showed **"275 tasks" inches above "857 Finished"** (275 live files, 620 archived). Now **867 everywhere**. (2) **Acceptance "(unnamed)" fix** — when a verify-agent `criteria[]` entry has no `criterion` name, the `notes` text is **promoted to the description** instead of rendering literal "(unnamed)"; styler's acceptance surface was **26/26 `✓ (unnamed) — …`** rows. This is the surface DEC-022 made the *authoritative* phase-acceptance status, so its legibility is load-bearing. (3) **Soft truncation** — new `_clip(text, limit, word=True)` helper (word-boundary cut + a real `…` + the full string in a `title=` / SVG `<title>` tooltip) replaces the hard `[:80]` / `[:64]` / `[:22]` mid-word slices that **dropped text from the DOM entirely** (unrecoverable even by selecting). Applied to acceptance notes (≤240), recent-task titles (≤90), and dependency-graph node labels (hard-clip 22 + hover `<title>` for the full label). (4) **Dependency-graph arrowheads** — `marker-end` on every edge (a heavier marker for the critical path) so dependency *direction* is explicit, not merely inferred from left→right layering. (5) **Merged hero ring** — the former completion-ring + status-donut (two near-identical circles at high completion — both ~all-green at 99%) collapsed into **one** status-segmented ring with `% complete` in the center and the per-status legend beside it (Erik's call over keep-both / drop-donut-keep-legend). `_donut` removed; `_ring` now takes `(segs, frac)`. (6) **Collapsible + height-capped Notes** — the unbounded Notes card (**1174px → 25%+ of the page** on styler) wrapped in a `<details open>` with a 340px `max-height` scroll box (`.notesblock` / `.notescard` CSS), so it stops dominating; open-by-default keeps the Quick Links visible, and the `collapse ▴ / expand ▾` affordance lets the user fold it. (7) **Self-generating spec quick-link** — the Notes card now **auto-prepends a live `📄 Spec: spec_v{N}.md` link** derived from the current spec version each regen, so it can never go stale; the first-regen seeding guidance (`dashboard-regeneration.md § "Notes first-regeneration seeding"`) is updated to **stop hand-authoring** a `spec_v{N}.md` path — the rot source the review surfaced (tinder's `user_notes` still pointed at `spec_v4.md` after the spec reached v5; the auto-link now renders `spec_v5.md` above it, and the Specification card already link-out the live spec). **Files touched (5):** `.claude/scripts/dashboard-render.py`, `.claude/scripts/tests/test_dashboard_render_html.py` (**+8 regression tests** — task-count-incl-archived, note-promotion, soft-clip-with-title, overlong-clip-with-title-fallback, graph-title+arrowheads, merged-ring-segments, notes-collapsible, notes-autoprepends-live-spec-link; suite **76 → 84 green**), `.claude/support/reference/dashboard-regeneration.md` (seeding guidance), `.claude/version.json` (5.0.0 → 5.1.0), `template-maintenance/ship-log.md`. MINOR per SemVer (new dashboard behaviors — merged ring, collapsible notes, arrowheads — plus the count / criteria / truncation bug fixes; additive — no command, task-schema, or permission change; no doctrine/`rules` edits). **No DEC** (renderer polish *within* DEC-024's HTML target; DEC-024 stays `implemented` with no open follow-ups). No sync-manifest change (`.claude/scripts/*` already covered). **Visual-verified** in headless Chromium against live styler + tinder data: 867-consistent counts, full-sentence acceptance criteria, edge arrowheads, one merged ring (6 segments, no `.donwrap`), Notes capped 338px (scrollHeight 1174). The 11 downstream dashboards pick the fixes up via the `template_version` format-staleness trigger on next `/work`. **Reviewed-but-not-actioned** (judged non-bugs): the frosted sticky header (86% + `backdrop-filter` blur — fine in a real browser; the apparent ghosting was an element-screenshot artifact) and styler's phase-25 numbering gap (correct data — phase 25 doesn't exist). (The stale-spec-link rot the review surfaced is now fixed by item (7) above; the remaining hand-authored `spec_v4.md` in tinder's `user_notes` is user content the user can prune, now superseded by the always-current auto-link.)
- **v5.1.1 (2026-07-19)** — **Template-repo sentinel migration + `system-overview.md` retirement.** Repo-type detection moved from "`system-overview.md` exists at project root" to "a `template-maintenance/` directory exists at project root" in all 4 sentinel sites: `commands/health-check.md` Part 5 (intro + § "Repo-type skip"), Part 5d (intro + step 1), Part 7 (Detection) + root `scripts/pre-commit-hook.sh` top guard. This unblocked deleting `system-overview.md` (stale since 2026-04-17, ~DEC-006 era — it predated `/grill`, `/shakedown`, `/diagnose`, `/zoom-out`, the audit family, the scripts family, the HTML dashboard, and DEC-016→024; its header still claimed "source of truth" and instructed consistency checks against itself, an active misinformation vector). Superseded by the layered truth model: shipped files = behavior truth; `decisions/` + `ship-log.md` + git tags = change truth; `template-maintenance/architecture-map.md` (new, 2026-07-19) = topology truth. Historical content: `git show v5.1.0:system-overview.md`. Downstream behavior unchanged — projects have neither sentinel, so sync/bridge parts run exactly as before (**PATCH**: internal detection mechanics only, no feature/interface change). No DEC — maintenance-model change recorded in root `CLAUDE.md § Template Maintenance Workflow` + the map. Companion maintenance-side changes (previous commit + this one): architecture map created; pre-commit hook gained a warn-only map-freshness check on topology-shaped changes; 83 git tags backfilled (`v1.0.0`→`v5.1.0`, first commit of each `template_version`) and per-ship tagging added to the ship definition-of-done; root `README.md` + `template-maintenance/README.md` directory listings updated (the latter now documents the folder's sentinel role). Verified: `bash -n` on the hook; live dry-run of both hook checks against a staged structural change; sentinel greps confirm zero remaining live `system-overview.md` references (historical mentions in `decisions/` + `feedback-archive.md` intentionally untouched). Also corrected during this pass: an earlier evaluation claim that `health-check.md` line ~595 carried a dangling `reference/new-feature.md` link — false positive (it's a placeholder filename inside an example sync-output block), no fix needed.
